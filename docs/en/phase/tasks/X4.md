# X4 — Phase-40 ASC prerequisite (d) sufficient-contrast verification audit

## Status

`READY_FOR_CONSOLIDATION`

## Scope

Audit-only deliverable closing Phase-40 ASC prerequisite (d) "sufficient-contrast verification across Liquid Glass tokens in both light + dark" (tech-debt.md:64). Measures WCAG 1.4.3 (Contrast Minimum) + 1.4.11 (Non-text Contrast) ratios for every Skeinly-rendered foreground × background pair across Material 3 baseline defaults (Compose), SwiftUI system colors + iOS 26 Liquid Glass styling, and the project-defined `AccentColor` (#7B61FF light / #9B82FF dark). Out of scope: no `*.kt` / `*.swift` / ADR / token-file edits; remediation work (if any required) is deferred to R6 as a separate worker slice per the task brief.

## Declared write-set

- `audits/contrast-verification-2026-05-19.md` (new file, sole deliverable)
- `docs/en/phase/tasks/X4.md` (this file)

## ADR / Spec refs

None edited. Audit references:
- ADR-025 `chart-canvas-accessibility.md` (read-only) — for cross-referencing R-series state-not-color guarantee on chart overlays.
- `audits/a11y-label-coverage-2026-05-17.md` (read-only) — format precedent.

## Test delta

+0 (document-only). Verification: see Result summary below — `make ci-local` exhibited the documented `ci-known-limitations.md:46` "iOS XCUITest 'Test crashed with signal kill' multi-test cluster" flake across 5 attempts; same SHA passes remote CI 2026-05-19T01:57Z. Per documented `document-and-tolerate` precedent (R1c + R2 workers pushed under identical conditions), the audit ships.

## Result summary

**Deliverable shipped**: `audits/contrast-verification-2026-05-19.md` — a 26-pair WCAG 1.4.3/1.4.11 contrast matrix across light + dark themes, with embedded reproducible Python computation script, R-series cross-references for state-not-color exemptions, and a paste-ready verdict for the tech-debt.md ASC bullet.

**Audit findings** (full detail in `audits/contrast-verification-2026-05-19.md`):
- **25 of 26 pairs PASS** — Material 3 baseline tokens engineered for WCAG AA; Skeinly inherits compliance unmodified (`SkeinlyTheme.kt:13-14` uses `lightColorScheme()`/`darkColorScheme()` with no overrides).
- **1 BLOCKER**: light-theme `AccentColor` `#7B61FF` as text on white = **4.20:1** (fails WCAG 1.4.3 normal text ≥ 4.5:1 by 0.30 ratio). Passes 1.4.11 UI (≥ 3.0). 6 call sites across 5 iOS screens. Dark theme variant (`#9B82FF` on `#000000` = 7.03:1) passes both thresholds.
- **11 numerical 1.4.11 fails are EXEMPT** under state-not-color (R5 onboarding ring + LiveRegion; SymbolPaletteStrip border-width delta + `selected` semantics; ChartComparisonAccessibility per-row spoken label naming `added`/`removed`/`modified`) or pure-decoration (chart grid backdrop).

**Verdict**: ⚠ DECLARABLE-WITH-REMEDIATION. **5/6 ASC declarations safely declarable today** (ダークインターフェイス + 視差効果を減らす + カラー以外で区別 + さらに大きな文字 + VoiceOver). 十分なコントラスト deferred pending R6 (AccentColor light variant remediation — 3 viable approaches documented in audit §6: darken / restrict-to-UI / large-text-only).

**Verification**:
- `make ci-local` Gradle chain (ktlintCheck + compileTestKotlinIosSimulatorArm64 + testAndroidHostTest + koverVerify + verifyI18nKeys) and `make ios-build` PASSED (3× `BUILD SUCCESSFUL` in /tmp/x4-ci-local-run1.log).
- `make ios-test` failed across 5 retry attempts with the documented `docs/en/phase/ci-known-limitations.md:46` "Test crashed with signal kill multi-test cluster" pattern: variable failing-test set per run (different tests fail each time, single-test re-runs pass), all failing test names are subset of the documented R1c/R2 observation list (OnboardingUITests + NavigationFlowUITests + ProjectDetailUITests + ProjectListUITests + IconButtonAccessibilityTests families). Hard-erased simulator (`xcrun simctl erase all`) did not change the pattern, confirming jetsamd-memory-pressure SIGKILL hypothesis rather than test-code issue. Same SHA (4fc170b) passes remote CI (`gh run list` 2026-05-19T01:57Z: CI / E2E Tests / Security all `success`). X4 is doc-only — `audits/contrast-verification-2026-05-19.md` + `docs/en/phase/tasks/X4.md` are the only modified files (verified via `git status --short` pre-push), so the cluster's stochastic test set cannot be caused by X4. Per the documented `document-and-tolerate` precedent (R1c worker `8ca758d` + R2 worker `5be0034` shipped under identical conditions), the audit pushes.

**Push**: SHA `7cf819fe3747805bf4fb0397db22ebbdc8b3db6d` on origin/main (2026-05-19). Single commit; full Conventional message body in `git log 7cf819f -1`.

## Follow-ups

1. **R6 candidate (real BLOCKER from audit §5.1 / §6)**: AccentColor light variant remediation to close ASC declaration #6 (十分なコントラスト). Three approaches documented in audit §6 (darken `#7B61FF` toward `#6B50EB`-class luminance / restrict `Color.accentColor` text usage to dark theme + UI components only / restrict to large-text only). ADR + worker assignment in next batch.
2. **ci-known-limitations.md:46 re-check observation**: X4 (2026-05-19) is an additional `last-checked` datapoint for the "Test crashed with signal kill multi-test cluster" entry — 5 retry attempts (including hard `simctl erase all`) reproduced the flake pattern but never with the same failing-test set, consistent with the documented hypothesis. The entry's current `Re-check by: 2026-06-18` cadence remains appropriate; orchestrator may extend `last-checked` to `2026-05-19` at consolidation. If the cluster becomes chronic before then, the proposed `xcrun simctl shutdown <UDID>` between test files OR workflow-level retry wrapper interventions remain on-deck.

## Task Result (orchestrator-consumed handoff block)

```
TASK RESULT
TASK-ID: X4
STATUS: READY_FOR_CONSOLIDATION
FILES CHANGED:
audits/contrast-verification-2026-05-19.md
docs/en/phase/tasks/X4.md
WRITE-SET RESPECTED: YES — exactly the 2 declared paths; no out-of-set writes
TEST RESULTS: +0 (document-only); Gradle invariant chain (ktlintCheck + compileTestKotlinIosSimulatorArm64 + testAndroidHostTest + koverVerify + verifyI18nKeys) + make ios-build PASS; make ios-test hit the documented ci-known-limitations.md:46 "Test crashed with signal kill multi-test cluster" flake across 5 attempts (variable failing-test set, all in the documented R1c/R2 observation list); same SHA 4fc170b passes remote CI 2026-05-19T01:57Z (CI / E2E / Security all green); doc-only scope cannot cause XCUITest failures; document-and-tolerate per R1c/R2 precedent
I18N FRAGMENT: none
PUSHED: 7cf819fe3747805bf4fb0397db22ebbdc8b3db6d (origin/main, 2026-05-19) — single commit `docs(audit): contrast-verification audit for Phase-40 ASC prerequisite (d) [X4]`
REVIEW: self-reviewed; audit follows audits/a11y-label-coverage-2026-05-17.md precedent (severity legend / declaration matrix / parity table / per-finding inventory / remediation backlog / verdict / maintenance); all 1.4.3 and 1.4.11 thresholds applied per WCAG 2.1/2.2 AA spec; embedded Python computation script reproduces every matrix value
RECOMMENDATION: SHIP — audit closes Phase-40 ASC prerequisite (d) audit-side; orchestrator folds verdict into tech-debt.md ASC bullet (5/6 → ⚠ DECLARABLE-WITH-REMEDIATION) at consolidation; opens R6 backlog (AccentColor light variant remediation) in roadmap.md or tech-debt.md
NOTES: 5 make ios-test retry attempts logged in /tmp/x4-ios-test-retry*.log + /tmp/x4-ci-local-run1.log (last lines of each capture variable cluster failures, all subset of ci-known-limitations.md:46 observation list). Hard-erased simulator (xcrun simctl erase all between attempts 4 and 5) did not change pattern, confirming jetsamd-memory-pressure hypothesis. No new upstream issue search needed — already-tracked entry covers this exact signature. Orchestrator should also extend ci-known-limitations.md:46 `last-checked` to 2026-05-19 at consolidation.
```
