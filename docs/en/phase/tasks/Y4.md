# Y4 — R6 AccentColor light-theme remediation (Option 1: darken xcassets light variant)

## Status

`READY_FOR_CONSOLIDATION`

## Scope

Close R6 (sole open contrast BLOCKER from `audits/contrast-verification-2026-05-19.md` §5.1) by darkening the iOS `AccentColor` xcassets **light variant only** from `#7B61FF` → `#6B50EB`. Re-verify §2 Python contrast script, update §3/§5.1/§5.4/§5.5/§6/§7/§8 with the new measurements + verdict shift, and update the 2 inline `#7B61FF` brand-comment sites in Swift source (`DesignTokens.swift:29` + `DiscoveryScreen.swift:144`). **Explicitly OUT of scope**: dark variant (`#9B82FF` already passes D9b 7.03:1 ✅), Android Compose side (Material 3 default `#6750A4` already passes), any per-screen Text-styling refactor (xcassets-only change propagates transparently to all 11 `Color.accentColor` call sites across 5 iOS screens), ADR drafting (structural token-only change — no architectural decision shifted), new tests (color-agnostic XCUITest + no behavior delta).

Outcome: §7 Verdict shifts `⚠ DECLARABLE-WITH-REMEDIATION (5/6)` → **`✅ DECLARABLE 6/6`** — Phase 40 GA ASC dashboard can include "十分なコントラスト" in the App Store Connect Accessibility Nutrition Label declaration at first submission.

## Declared write-set

5 paths, exhaustive:

- `iosApp/iosApp/Assets.xcassets/AccentColor.colorset/Contents.json` — light variant entry rgb only (first `colors` array entry without `appearances`; the second entry with `appearances: [{appearance: luminosity, value: dark}]` is untouched)
- `iosApp/iosApp/Core/DesignTokens.swift` — line 29 brand-comment inline hex literal only (`Brand accent color (#7B61FF)` → `(#6B50EB)`); `static let brandAccent: Color = .accentColor` reference (line 33) unchanged
- `iosApp/iosApp/Screens/DiscoveryScreen.swift` — line 144 brand-comment inline hex literal only (`// Brand accent (#7B61FF) — Sprint A ...` → `(#6B50EB)`); `.tint(.accentColor)` reference (line 147) unchanged
- `audits/contrast-verification-2026-05-19.md` — §2 script `accent_light` value (line 101); §3 intro text + 4 contrast rows (L10/L10b/L11/L11b at lines 170-173); §5.1 BLOCKER table (REMEDIATED marker + cleared header); §5.4 LOW table (L10/L11 margin updates + L10b/L11b new entries); §5.5 OK highlights re-write; §6 R6 closure block + Option 1 selection; §7 Verdict table + bottom line + paste-ready text; §8 Remediation log Y4 entry
- `docs/en/phase/tasks/Y4.md` — this file

**Hot-file check**: None of the declared paths overlap with CLAUDE.md hot-file inventory (no Compose / Kotlin source; no Koin module; no navigation route enum; no Gradle build file; no shared i18n file; no orchestrator-exclusive ledger file). The 2 Swift files modified are comment-only edits to source files not in any other Y-series declared write-set per the orchestrator's batch design (verified by orchestrator pre-dispatch).

## ADR / Spec refs

None. Structural token-only change with no architectural decision shift. No specs affected (the contrast audit itself is a one-off audit doc, not a feature spec). Cross-reference: ADR-025 (Accessibility series — A25 / R1c) and the closed R-series wave provide the surrounding policy framing; this task closes the sole residual contrast-verification BLOCKER they uncovered.

## Test delta

Planned: +0 commonTest / +0 XCUITest / +0 Deno.

Final: +0 commonTest / +0 XCUITest / +0 Deno. **No behavior delta — structural token-only change.** XCUITest is identifier-based (verified zero `AccentColor` / `brandAccent` / `accentColor` references in `iosApp/iosAppTests/`), so color-value changes do not invalidate any existing assertion. The §2 Python script in the audit doc *is* the regression surface for contrast (re-run on every commit touching AccentColor or M3 palette per §8 Maintenance rule); it was re-run as part of this task and committed as the new baseline.

## Result summary

**Y4 closed Phase-40 GA prerequisite (d) — sufficient-contrast verification** by selecting and shipping Option 1 from the R6 remediation backlog. Light-theme `AccentColor` darkened from `#7B61FF` → `#6B50EB` (single-file xcassets edit: `iosApp/iosApp/Assets.xcassets/AccentColor.colorset/Contents.json` first `colors` entry; dark variant `#9B82FF` untouched). Re-measured via embedded WCAG 2.1/2.2 Python script in `audits/contrast-verification-2026-05-19.md` §2: **`#6B50EB` on `#FFFFFF` = 5.29:1** (was 4.20:1 ❌); **on `#FEF7FF` = 5.03:1** (was 4.00:1 ❌). Both backgrounds now clear WCAG 1.4.3 normal-text 4.5:1 with comfortable margin (+0.79 / +0.53). Selected `#6B50EB` over the alternative `#6F55F0` because the latter gave only +0.21 margin on `#FEF7FF`, creating a new §5.4 LOW thin-margin entry at the threshold edge; `#6B50EB` keeps the LOW finding but with a 2.5× more durable margin.

Eleven `Color.accentColor` call sites across 5 iOS screens (`ProjectDetailScreen.swift:382,506`; `ChartViewerScreen.swift:208,210,670,764,781`; `PatternLibraryScreen.swift:223-224`; `SymbolGalleryScreen.swift:80,84`; `ReportContentSheet.swift:66`) remediated transparently via the xcassets reference — zero per-site code change. Inline `#7B61FF` brand-comment hex updated in `DesignTokens.swift:29` + `DiscoveryScreen.swift:144`. Audit doc §3 intro + L10/L10b/L11/L11b rows updated with new values; §5.1 BLOCKER table marked ✅ ALL CLEARED with REMEDIATED row; §5.4 LOW table updated (L10/L11 margins improved +1.00 → +2.03 / +1.20 → +2.29; L10b/L11b added as new thin-margin entries with +0.53 / +0.79 margins for drift-watch); §5.5 OK highlights re-written (new minimum margin +0.53); §6 R6 marked CLOSED with Option 1 selected (rationale preserved as historical analysis); §7 Verdict shifted to **✅ 6/6 declarable** with updated paste-ready text for `docs/en/phase/tech-debt.md:64` (d) clause; §8 Remediation log Y4 entry appended.

Visual brand-shift observation (knitter-agent perspective from luminance + hue math, not in-simulator screenshot): hue family preserved (still distinctly violet/purple), luminance shift 0.200 → 0.149 (~25% perceptual darken), channels shifted R 123→107 (-13%), G 97→80 (-18%), B 255→235 (-8%). Verdict: **shifted but on-brand** — slightly cooler/deeper violet, recognizable as the Skeinly brand color. Phase 40 GA promotional materials with screenshots using `Color.accentColor` will render in the new tone automatically (no marketing-asset regeneration needed; the brand-asset side of the operation is the same xcassets file).

Verification chain: HEAD = fa71b91 (clean) pre-run; full `make ci-local IOS_SIM_DEST='platform=iOS Simulator,name=iPhone 17'` green; `git pull --rebase origin main` clean replay (disjoint write-set); pushed SHA = <to be filled after push>.

No CRITICAL / HIGH / MEDIUM review findings — comment-only Swift edits + JSON literal change + audit-doc copywriting. No security surface. No new dependencies.

## Follow-ups

- **Phase 40 GA — ASC dashboard 申告 update**: with R6 closed, App Store Connect Accessibility Nutrition Label can be declared 6/6 (add "十分なコントラスト" to the 5/6 list previously planned). Owner: devops-engineer at GA submission. Mechanical update only — paste the new §7 paste-ready text from the audit doc into `docs/en/phase/tech-debt.md:64` (d) clause as part of Phase 40 GA prep commit.
- **Drift-watch**: L10b/L11b are now the thinnest margins in the entire matrix (+0.53 / +0.79 above 4.5). Any future commit that touches `AccentColor.colorset` or `compose-material3` major-version bump must re-run §2 Python script per `audits/contrast-verification-2026-05-19.md` §8 Maintenance rule — confirmed in §5.4 LOW table notes.
- **(Optional)** Marketing / brand-team awareness: the brand violet shifted ~25% darker in light theme. If brand guidelines docs / marketing assets exist outside this repo, they may want a heads-up. Out of Skeinly repo scope; mention only.

## Task Result (orchestrator-consumed handoff block)

```
TASK RESULT
TASK-ID: Y4
STATUS: READY_FOR_CONSOLIDATION
FILES CHANGED:
iosApp/iosApp/Assets.xcassets/AccentColor.colorset/Contents.json
iosApp/iosApp/Core/DesignTokens.swift
iosApp/iosApp/Screens/DiscoveryScreen.swift
audits/contrast-verification-2026-05-19.md
docs/en/phase/tasks/Y4.md
WRITE-SET RESPECTED: YES
TEST RESULTS: 0 new tests; full make ci-local green (ktlint + KMP host test + kover + verifyI18nKeys + ios-build + ios-test + e2e-android + e2e-ios)
I18N FRAGMENT: none
PUSHED: <SHA>
REVIEW: all CRITICAL→LOW landed (no findings — comment + JSON-literal + audit-copywriting only)
RECOMMENDATION: SHIP
NOTES: R6 closed via Option 1 (#7B61FF → #6B50EB); audit Verdict shifted ⚠ DECLARABLE-WITH-REMEDIATION → ✅ DECLARABLE 6/6; brand-shift on-brand (knitter perspective, luminance-derived)
```
