# X2 — R2 Tech Debt (vi): Compose ChartImageThumbnail Role.Button + selected polish

## Status

`READY_FOR_CONSOLIDATION`

## Scope

Close tech-debt **R2 (vi)** by annotating the Compose `ChartImageThumbnail` (private composable inside `ChartImageGrid.kt`) with `Modifier.semantics { role = Role.Button; if (isSelected) selected = true }`. Lift an optional `selectedIndex: Int? = null` to the public `ChartImageGrid` for forward-compat so a future task can wire `ProjectDetailState.selectedChartImageIndex` without re-touching this file. Annotation-only — no caller edited, no new i18n keys, no behavior change (current callers omit `selectedIndex` ⇒ default `null` ⇒ `isSelected` always false, identical to today's screen-reader output except for the always-on `Role.Button` trait, which is the win).

Explicit out-of-scope: wiring `selectedIndex` from any caller (`*Screen.kt` is forbidden in write-set per worker prompt), iOS counterpart (none exists; tech-debt entry confirms H5 audit row was iOS-only and the Compose polish stands alone), test additions (no existing `ChartImageGrid` test file; orchestrator allowance `+0/+0/+0 OK` for annotation-only).

## Declared write-set

- `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chartviewer/ChartImageGrid.kt`
- `docs/en/phase/tasks/X2.md`

## ADR / Spec refs

None directly. Implements the closure criteria for the **tech-debt.md R2 (vi)** entry (orchestrator-exclusive file; this worker does not edit it). No ADR `Revision history` touched. No spec file in scope.

## Test delta

Planned: **+0 commonTest / +0 XCUITest / +0 Deno**. Final: **+0 / +0 / +0** (as expected — annotation-only).

Rationale: no existing `ChartImageGrid` test file exists in `shared/src/commonTest/` or `androidUnitTest/`. Per orchestrator prompt: "既存 ChartImageGrid テストがある場合 selected state semantic assertion 追加(+1 commonTest 想定)。なければ +0 / +0 / +0 で OK(annotation-only)。"

## Result summary

**Shipped**: Compose `ChartImageThumbnail` (inside `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chartviewer/ChartImageGrid.kt`) now carries `Modifier.semantics { role = Role.Button; if (isSelected) selected = true }` on its outer Box. Added optional `selectedIndex: Int? = null` to public `ChartImageGrid`; `isSelected: Boolean = false` to private `ChartImageThumbnail`. `isSelected = index == selectedIndex` is passed from the `itemsIndexed` loop. Added 4 imports: `Role`, `role`, `selected`, `semantics` (all under `androidx.compose.ui.semantics`).

**Key design decisions**:
- Added `selectedIndex` to the public surface (not just to the private composable) so the `selected` semantic is reachable via the public API. Default `null` keeps current callers (`ProjectDetailScreen.kt` lines 482–491 and 497–504, NOT edited this task — out of declared write-set) byte-compat. Forward-compatible for a follow-up task to wire `state.selectedChartImageIndex` from `ProjectDetailState` without re-touching this file.
- `Role.Button` is always on (the right answer — these ARE clickable buttons; TalkBack now announces "button" instead of only the image content description).
- `selected` only flips when caller passes a matching index; harmless when omitted.

**Scope cuts**: wiring `selectedIndex` from `ProjectDetailScreen` was deferred (`*Screen.kt` is forbidden in worker write-set per orchestrator prompt). Surfaced as a follow-up (Follow-ups section). Revisit at the next batch when a worktree's write-set includes `ProjectDetailScreen.kt`.

**Test delta**: +0 commonTest / +0 XCUITest / +0 Deno. Running totals unchanged.

**Review findings landed**: none introduced — annotation-only, no new logic. ktlint green on first pass (4 imports inserted alphabetically into the existing alphabetized block).

**Verification run** (with X2 in place, from worktree root):
- `:shared:ktlintCheck` ✓
- `:androidApp:ktlintCheck` ✓
- `:shared:compileTestKotlinIosSimulatorArm64` ✓
- `:shared:testAndroidHostTest` ✓
- `:shared:koverVerify` ✓
- `verifyI18nKeys` ✓
- `make ios-build` ✓ (Xcode 26.5, `IOS_SIM_DEST` override → iPhone 17 because no iPhone 16 simulator is installed locally)
- `make ios-test`: first run 7× **Test crashed with signal kill** (matches [`ci-known-limitations.md` L46](../ci-known-limitations.md) — same uniformly-stochastic signature first observed by R1c/R2 workers on 2026-05-18; single-run rerun consistently green per the documented trail). **Rerun: 21/21 PASS** ✓
- `make e2e-android` ✓ 9/9 PASS in 6m 7s
- `make e2e-ios`: **SKIPPED on host** per [`ci-known-limitations.md` L31](../ci-known-limitations.md) — Maestro 2.5.x iOS driver broken on host Xcode 26.x ([upstream Maestro#3218](https://github.com/mobile-dev-inc/Maestro/issues/3218), OPEN, last-checked 2026-05-06). "Host iOS E2E is grey while CI gates the same suite." X2's annotation-only Kotlin Compose change cannot affect Maestro flows.

**Failure-isolation evidence** (proving the SIGKILL cluster was orthogonal to X2):
1. With X2 stashed (clean baseline at `4fc170b`), `make ios-test` passed **21/21 in 297s** on iPhone 17 / iOS 26.5.
2. With X2 reapplied, rerun of `make ios-test` passed **21/21 in 293s** on the same simulator. The intervening first-run failure was therefore the documented transient flake, not a deterministic regression from X2.

**Pushed SHA**: `865ff61fc8eef24f360490e4ae3e4a00b026f40a` on `origin/main` (push line: `4fc170b..865ff61 HEAD -> main`).

## Follow-ups

- A future task should wire `state.selectedChartImageIndex` from `ProjectDetailScreen.kt` (lines 482–491 call site, when images present) through the new `selectedIndex` parameter on `ChartImageGrid`. Out of scope this task because `*Screen.kt` is forbidden write-set; not blocking R2 (vi) closure. Orchestrator may surface this into `tech-debt.md` at consolidation.

## Task Result (orchestrator-consumed handoff block)

```
TASK RESULT
TASK-ID: X2
STATUS: READY_FOR_CONSOLIDATION
FILES CHANGED: shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chartviewer/ChartImageGrid.kt
               docs/en/phase/tasks/X2.md
WRITE-SET RESPECTED: YES
TEST RESULTS: 21/21 iOS XCUITest PASS (rerun after one transient SIGKILL-cluster flake matching ci-known-limitations.md L46); 9/9 e2e-android PASS; shared+android host tests + koverVerify + verifyI18nKeys + ktlintCheck green. Running totals unchanged (+0 commonTest / +0 XCUITest / +0 Deno).
I18N FRAGMENT: none
PUSHED: 865ff61fc8eef24f360490e4ae3e4a00b026f40a on origin/main
REVIEW: all CRITICAL→LOW landed (annotation-only; no new logic introduced)
RECOMMENDATION: SHIP
NOTES: Closes tech-debt R2 (vi). selectedIndex param added to public ChartImageGrid as forward-compat; wiring state.selectedChartImageIndex from ProjectDetailScreen.kt deferred to a follow-up task (*Screen.kt out of worker write-set — orchestrator to add to tech-debt.md if desired). make e2e-ios skipped on host per ci-known-limitations.md L31 (Maestro 2.5.x iOS driver broken on host Xcode 26.x; CI gates the suite server-side); change is annotation-only Kotlin Compose and cannot affect Maestro flows. IOS_SIM_DEST override → iPhone 17 used because iPhone 16 simulator not installed locally (Makefile L9 supports override; default unchanged).
```
