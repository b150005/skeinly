# Z1 — ADR-027 doc drafting: UGC moderation triage automation via GitHub Issue webhook (DRAFT)

## Status

`IN_PROGRESS`

## Scope

Draft ADR-027 only (doc work). Evolve ADR-021 §D5 by introducing a new ADR (rather than amending §D5 in place) that automates the operator triage flow: GitHub Issue close → repo-level webhook → Edge Function `github-webhook` → HMAC verify → label-based decision mapping → `UPDATE public.ugc_reports SET state = …, resolved_at = issue.closed_at WHERE id = … AND resolved_at IS NULL`. ADR-021 §D5's manual dual-sync stays as the fallback path.

Add a 1-line evolution cross-link from ADR-021 §D5 to ADR-027, plus a new Revision history row noting the cross-link addition. JA mirrors of both ADR-027 and the ADR-021 edit are mandatory (CLAUDE.md `## Documentation Convention`).

Implementation slice (Edge Function code, `supabase/config.toml` wiring, `docs/en/ops/ugc-moderation-sop.md` updates, `docs/en/ops/release-secrets.md` `GITHUB_WEBHOOK_SECRET` slot, GitHub repo settings) is OUT of scope — deferred to a later worker batch (Z2 placeholder).

## Declared write-set

- `docs/en/adr/027-ugc-triage-automation.md` (NEW, source of truth EN)
- `docs/ja/adr/027-ugc-triage-automation.md` (NEW, JA mirror)
- `docs/en/adr/021-pre-alpha-ugc-moderation.md` (EDIT — 1-line cross-link in §D5 + 1 new row in Revision history table)
- `docs/ja/adr/021-pre-alpha-ugc-moderation.md` (EDIT — equivalent JA additions)
- `docs/en/phase/tasks/Z1.md` (this file, always)

## ADR / Spec refs

- ADR-027 — new ADR drafted by this task
- ADR-021 §D5 — cross-link addition + Revision history append
- ADR-020 §Q4 — `verify_jwt = false` precedent referenced from ADR-027 D2
- `docs/en/ops/ugc-moderation-sop.md` — referenced from ADR-027 (Z2 will update this file, not this worker)
- No spec under `docs/en/spec/` covers UGC moderation; this is ADR-only territory.

## Test delta

Planned: none (doc-only task; no commonTest / XCUITest / Deno test changes).
Final: none (zero test delta; running total unchanged).

## Result summary

<!-- Filled when Status → READY_FOR_CONSOLIDATION; see bottom of file. -->

## Follow-ups

<!-- Filled at READY_FOR_CONSOLIDATION; surfaced via TASK RESULT block. -->

## Task Result (orchestrator-consumed handoff block)

<!-- Emitted verbatim at the end of the worker session. See _TEMPLATE.md schema. -->
