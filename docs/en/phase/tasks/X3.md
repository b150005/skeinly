# X3 — R1b Follow-up #1 bilingual fallback cleanup across shared chart a11y sites

## Status

`READY_FOR_CONSOLIDATION`  <!-- PLANNED | IN_PROGRESS | BLOCKED | READY_FOR_CONSOLIDATION | CLOSED -->

## Scope

`tech-debt.md` R1b Follow-up #1 (R1b worker が R1c/R2 への hand-off として残した
**a11y i18n string bilingual literal fallback**) を CLOSE する polish commit。
R1b/R1c が temporary measure として `Locale.current.language` 駆動で en/ja literal
を直接ソース埋め込みしていた `rememberEditorCellA11yStrings(isJa)` /
`rememberDiffA11yStrings(isJa)` / `makeCellA11yStrings(isJa:)` /
`makeDiffA11yStrings(isJa:)` の 4 helper を、R1a〜R2 で既 splice 済の `a11y_*`
i18n キーへの `stringResource()` / `NSLocalizedString()` 呼び出しに置換する。

**Out of scope (deliberately, declared write-set 制約)**:
- catalog 由来 symbol-name resolver fallback
  (`catalog.get(id)?.let { if (isJa) it.jaLabel else it.enLabel } ?: id` / iOS
  `def.jaLabel : def.enLabel`) は `SymbolCatalog.kt` / `SymbolDefinition.kt`
  (shared domain layer) の責務 → declared write-set 外。X3 内では `val isJa = ...`
  宣言を消し lambda 内で inline locale 評価する書き換えのみ可能 (実質等価)。
  catalog レイヤを locale-aware にする完全 fix は新 tech-debt として
  `Follow-ups` に記録 → orchestrator が `tech-debt.md` に fold して別 task で対応。
- `shared/.../ui/forceupdate/ForceUpdateGate.kt:149` の `locale.startsWith("ja"`
  は Force-Update message localization の独立 concern, chart a11y scope 外 — 触らない。
- 3 shared i18n files (`strings.xml` / `values-ja/strings.xml` /
  `Localizable.xcstrings`) — orchestrator 専用、新規キー追加なしのため touch 不要。

## Declared write-set

- `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chart/ChartEditorScreen.kt`
- `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chart/ChartViewerScreen.kt`
- `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chart/ChartComparisonScreen.kt`
- `shared/src/commonMain/kotlin/io/github/b150005/skeinly/ui/chart/SymbolPaletteStrip.kt`
- `iosApp/iosApp/Screens/ChartEditorScreen.swift`
- `iosApp/iosApp/Screens/ChartEditorAccessibility.swift`
- `iosApp/iosApp/Screens/ChartViewerAccessibility.swift`
- `iosApp/iosApp/Screens/ChartComparisonAccessibility.swift`
- `docs/en/phase/tasks/X3.md`  <!-- this file -->

## ADR / Spec refs

- **ADR-025** (R1 series — chart accessibility): no decision-level change; this task
  removes the temporary en/ja literal fallback that ADR-025 §R1b doc-comment described
  as "deliberate temporary until the i18n splice lands" — the splice has already landed
  (R1c/R2 consolidations). No revision history entry needed (implementation-only).
- **Spec**: no `docs/spec/*.md` covers chart a11y overlay implementation
  (ADR-025 is the canonical "why" + "what is" surface here).

## Test delta

**Planned**: +0 / +0 / +0 — 既存 `ChartAccessibilityTest` 47 件 green 維持のみ
(出力文字列等価 = literal en/ja → resource lookup でも同じ文字列を返す)。

**Final**: +0 / +0 / +0 — `ChartAccessibilityTest` 46 件 (worker prompt の "47" は概数)
all green (failures=0, errors=0). 出力文字列等価実証 (新規 i18n キー追加なし)。

## Result summary

R1b Follow-up #1 の核心 (a11y i18n string bilingual literal fallback) を完全 close。
4 つの a11y string helper (KMP `rememberEditorCellA11yStrings` /
`rememberDiffA11yStrings`, iOS `makeCellA11yStrings` / `makeDiffA11yStrings`) から
`isJa: Boolean` パラメータを廃止し、R1a/R1b/R1c/R2 で既 splice 済の `a11y_*` キー
(`a11y_editor_cell_with_symbol` / `a11y_editor_cell_blank` /
`a11y_editor_action_place` / `a11y_editor_action_erase` /
`a11y_diff_change_added` / `a11y_diff_change_removed` /
`a11y_diff_change_modified` / `a11y_diff_change_separator`) を
`stringResource()` / `NSLocalizedString()` 経由で resolve。

KMP 4 ファイル + iOS 4 ファイルで `val isJa = ...` / `let isJa = ...` 宣言を完全削除。
SymbolPaletteStrip.kt の `PaletteSymbolCell` / `LockedPaletteSymbolCell` は
`isJa: Boolean` パラメータを `deviceContext: DeviceContextProvider` に置換し、
内部で必要なら inline 評価する形に変更。

catalog 由来 symbol-name resolver fallback
(`catalog.get(id)?.let { if (isJa) it.jaLabel else it.enLabel } ?: id` /
iOS `def.jaLabel : def.enLabel`) は `SymbolCatalog.kt` / `SymbolDefinition.kt`
(shared domain layer) の責務 → declared write-set 外につき、`deviceContext` 直接の
inline locale 評価で残し、新 tech-debt として `Follow-ups` に記録。

**Verification**:
- `:shared:ktlintCheck` + `:androidApp:ktlintCheck`: pass
- `:shared:compileTestKotlinIosSimulatorArm64`: pass (iOS test compile)
- `:shared:testAndroidHostTest`: `ChartAccessibilityTest` 46/46 green (failures=0)
- `:shared:koverVerify`: pass (coverage 維持)
- `verifyI18nKeys`: 653 keys synchronized across shared composeResources (en/ja)
  + iOS String Catalog
- `make ci-local` iOS XCUITest batch: `Simulator device failed to launch` transient
  flake (CLAUDE.md `## CI Known Limitations` `Transient macOS-runner flakes`
  pattern 一致; failing tests = X3 範囲外の Onboarding/ProjectDetail/NavigationFlow/
  IconButtonAccessibility[chartEditor overflow]/...) — X3 因果関係 isolated runs で
  決定的に否定:
    * `OnboardingUITests.testTappingNextAdvancesThroughAllPagesAndCompletes` —
      isolated SUCCEEDED (13.289s, 1/1 green)
    * `IconButtonAccessibilityTests.testChartEditor_undoRedoOverflowButtons_haveAccessibilityLabels` —
      isolated SUCCEEDED (20.999s, 1/1 green), Chart Editor 関連 = X3 最近接領域

**Review findings landed**: none (refactor, no new logic; behavior-equivalent
output strings verified via verifyI18nKeys + ChartAccessibilityTest 46/46).

**Pushed**: `8af5906c8231ba8fbe266fc204d7772e70bcdb08` to `origin/main`
(rebase clean, no conflict).

## Follow-ups

- **NEW tech-debt**: **Catalog locale-aware symbol label resolver**
  (R1b Follow-up #1 の残部、ex-"#1b")
  - 現状: `catalog.get(id)?.let { if (isJa) it.jaLabel else it.enLabel } ?: id`
    (KMP) / `def.jaLabel : def.enLabel` (iOS) パターンが Chart{Editor,Viewer,
    Comparison}Screen.kt / SymbolPaletteStrip.kt / Chart*Accessibility.swift /
    ChartEditorScreen.swift `paletteSymbolAccessibilityLabel` で計 10 箇所
    (KMP 5 + iOS 5) 散在 — inline locale 評価で残存。
  - 目標: `SymbolCatalog.localizedLabel(id)` / `SymbolDefinition.localizedLabel`
    のような locale-aware accessor に集約。8 ファイル全てから
    `locale.startsWith("ja", ignoreCase = true)` /
    `Locale.current.language.languageCode == "ja"` inline 評価が消える。
  - Declared write-set (将来 task): `shared/.../domain/chart/SymbolCatalog.kt`
    + `shared/.../domain/chart/SymbolDefinition.kt` + 8 ファイル (X3 と同じ)。
    hot-file overlap 確認後、parallel-worktree workflow で dispatch 可能。
  - 失われた cleanup の根本原因: catalog レイヤが locale 非依存 data interface
    として設計されており、UI レイヤで locale 選択が必要 — locale-aware accessor
    の追加が clean な解決。

