# Skeinly — KMP Knitting App

## About This Project

**Skeinly** is a Kotlin Multiplatform (KMP) mobile app for knitters. It helps users manage knitting patterns (charts), track progress on projects, and share patterns/progress with others.

- **Platforms**: Android (Jetpack Compose), iOS (SwiftUI), macOS (SwiftUI, stretch goal)
- **Architecture**: KMP shared logic + platform-native UI
- **Data Strategy**: Local-first with cloud sync (Firebase or Supabase — to be decided via ADR)
- **Language**: Kotlin (shared), Kotlin (Android UI), Swift (iOS/macOS UI)

Created from the [ECC Base Template](https://github.com/b150005/ecc-base-template).

## Project Structure

```
skeinly/
├── shared/                    # KMP shared module
│   ├── src/commonMain/        # Shared business logic, models, repositories
│   ├── src/commonTest/        # Shared tests
│   ├── src/androidMain/       # Android-specific implementations
│   └── src/iosMain/           # iOS-specific implementations (Kotlin)
├── androidApp/                # Android application (Jetpack Compose UI)
├── iosApp/                    # iOS/macOS application (SwiftUI, Xcode project)
├── docs/
│   ├── en/                    # English documentation (source of truth)
│   └── ja/                    # Japanese translations
└── .claude/                   # Agent team and Claude Code configuration
```

## Documentation map

Each doc has a deliberate lane. Match the question to the lane before reading or writing:

| Lane | Purpose | Where |
|---|---|---|
| **WHAT IS** | Current shape of the system / a feature | [docs/en/architecture.md](../docs/en/architecture.md) (system) + [docs/en/spec/](../docs/en/spec/) (per-feature) |
| **WHY** | Design rationale + alternatives considered | [docs/en/adr/](../docs/en/adr/) |
| **WHAT TO DO** | Step-by-step operator runbooks | [docs/en/ops/](../docs/en/ops/) |
| **HISTORICAL** | How we got here over time | [docs/en/phase/](../docs/en/phase/) + ADR revision histories |

When implementation drifts from an ADR, update the spec (or system architecture); only update the ADR if the *decision* changed (not just the implementation). The ADR's `Revision history` block is the trail for decision-level changes.

Quick entry points by task:
- Joining the project / coming back after a long break → `docs/en/architecture.md`
- Extending a feature → corresponding `docs/en/spec/<feature>.md`
- Operational task (publish content, cut a release, rotate a secret, debug a production failure) → `docs/en/ops/README.md` then the matching runbook
- Tracking decisions → `docs/en/adr/`

## Architecture Principles

- **Clean Architecture** with clear separation: UI → ViewModel → UseCase → Repository → DataSource
- **KMP shared module** contains: domain models, use cases, repository interfaces, and common utilities
- **Platform-native UI**: Android uses Jetpack Compose, iOS/macOS uses SwiftUI + UIKit (via `UIViewControllerRepresentable`/`UIViewRepresentable` where SwiftUI alone is insufficient)
- **Repository pattern** for data access abstraction (local DB + remote sync)
- **Immutable data structures**: use Kotlin `data class` with `copy()`, Swift structs
- **Dependency injection**: Koin for shared/Android, native DI patterns for iOS
- **Unidirectional data flow**: State flows down, events flow up

## Key Technology Stack

| Layer | Technology |
|-------|-----------|
| Shared Logic | Kotlin Multiplatform |
| Android UI | Jetpack Compose + Material 3 |
| iOS/macOS UI | SwiftUI + UIKit (interop as needed) |
| Local Database | SQLDelight (multiplatform) |
| Networking | Ktor Client (multiplatform) |
| Serialization | kotlinx.serialization |
| Async | Kotlin Coroutines + Flow (shared), Swift async/await (iOS) |
| DI | Koin Multiplatform |
| Image Loading | Coil (Android), platform-native (iOS) |
| Build System | Gradle (KMP), Xcode via XcodeGen (iOS) |

## Development Environment

### Required versions

| Component | Minimum | Source of truth |
|---|---|---|
| macOS | 26.0 (Tahoe) | [README.md](../README.md) Prerequisites |
| Xcode | 26.0 | [README.md](../README.md) Prerequisites; verified by `make verify-xcode` |
| JDK | 17 | Temurin recommended |
| Android SDK | API 36 | platform-tools (`adb`) required |
| iOS deployment target | 17.0 | [iosApp/project.yml](../iosApp/project.yml) `deploymentTarget.iOS` |

**Why Xcode 26 / macOS 26 / iOS 26 SDK?** Apple App Store Connect submissions since **2026-04-28** require apps built with Xcode 26+ using the iOS 26 SDK or later (also iPadOS 26 / tvOS 26 / visionOS 26 / watchOS 26 SDK as applicable). CI runners (`macos-latest` on GitHub-hosted) ship with Xcode 26+ since the enforcement date. Local builds must match. See [docs/en/ops/repo-policy.md](../docs/en/ops/repo-policy.md#apple-app-store-sdk-requirements) for the full requirement and references.

**Build SDK vs deployment target**: We build with the iOS 26 SDK (latest), but the runtime deployment target stays at iOS 17 — these are independent. iOS 26-only APIs must be gated with `if #available(iOS 26.0, *)` on Swift / `Build.VERSION.SDK_INT` checks on Android-side equivalents. The iOS 26 SDK applies Liquid Glass styling to native UIKit/SwiftUI controls by default; Phase 18 already adopted Liquid Glass design tokens, so no opt-out is required.

**Verification before release**: `make release-ipa-local` invokes `make verify-xcode` first to fail fast if Xcode is older than 26. Skip the check at your own risk; Apple's upload-time enforcement will reject the artifact downstream.

## Domain Model (Core Concepts)

- **Pattern**: A knitting pattern with metadata (name, difficulty, gauge, yarn info, chart/reference images)
- **Chart** (model: `Chart`): The symbol-grid editor surface — cells × layers × symbols. JA UI: 「編み図」.
- **Project**: An instance of working on a pattern (start date, current row/round, status)
- **Progress**: Row-by-row or section-based progress tracking with timestamps
- **Share**: Exported pattern or progress snapshot for sharing with others
- **Variation** (model: `ChartVariation`, table: `chart_variations`): A named parallel design forked off a base chart. JA UI: 「アレンジ」.
- **Version** (model: `ChartVersion`, table: `chart_versions`): An append-only saved snapshot of a chart. JA UI: 「バージョン」.
- **Suggestion** (model: `Suggestion`, table: `suggestions`): A proposed change to another user's chart. JA UI: 「提案」.

### Vocabulary mapping (Git mental model → user-facing language)

The collaboration model is a Git-shaped DAG internally, but user-facing language is knitter-friendly per the 2026-05-10 terminology audit (`audits/terminology-audit-2026-05-10.md`). When extending these surfaces, prefer the knitter-friendly term in user-visible copy + ADR text, while feel free to use Git mental models in code comments + ADRs' technical sections.

| Git concept | Skeinly user-facing (EN) | Skeinly user-facing (JA) | Internal class / table |
|---|---|---|---|
| fork (verb/noun) | Save a copy / Save to my library | コピーを保存 / マイライブラリに追加 | `SaveSharedPatternToLibraryUseCase` (no separate table; uses `Pattern.parentPatternId`) |
| branch | Variation | アレンジ | `ChartVariation` / `chart_variations` |
| commit / revision | Version | バージョン | `ChartVersion` / `chart_versions` |
| merge | Apply changes | 変更を反映 | `ApplySuggestionUseCase` + RPC `apply_suggestion` |
| pull request | Suggestion | 提案 | `Suggestion` / `suggestions` |
| diff | Comparison | 比較 | `ChartComparison` |
| upstream | original | 元の (e.g. 元のパターン) | (concept; no class) |

See [README.md](../README.md) `## Vocabulary mapping (developer reference)` for the same table reproduced for human contributors.

## Agent Team

This project uses an agent team for structured development. The **orchestrator** agent coordinates the following specialists:

| Agent | Role |
|-------|------|
| orchestrator | Analyzes issues, creates plans, delegates to specialists |
| product-manager | Product planning, PRD, user stories, acceptance criteria |
| market-analyst | Market research, competitor analysis |
| monetization-strategist | Business model, pricing, revenue strategy |
| ui-ux-designer | UI/UX design, accessibility, usability review |
| docs-researcher | Documentation research, API verification, freshness-safe search |
| architect | System architecture, technology decisions |
| implementer | Code implementation following architecture and TDD |
| code-reviewer | Code quality and standards review |
| test-runner | Test execution, coverage reporting |
| linter | Static analysis, code style enforcement |
| security-reviewer | Vulnerability detection, OWASP Top 10 |
| performance-engineer | Profiling, bottleneck identification, optimization |
| devops-engineer | CI/CD, deployment strategy, release management |
| technical-writer | Documentation, changelog, bilingual docs |
| knitter | Domain advisor on knitting symbols, bilingual labels, and craft conventions (first-pass reviewer; escalates conflicts to user) |

### Ecosystem Detection

Agents detect this project as **Kotlin Multiplatform** by finding:
- `build.gradle.kts` with KMP plugin configuration
- `shared/` module with `commonMain`, `androidMain`, `iosMain` source sets
- `androidApp/` with Jetpack Compose dependencies
- `iosApp/` with Xcode project and SwiftUI

## Development Workflow

> Branch protection, PR requirements, status check gates, and bypass mechanics are documented in [docs/en/ops/repo-policy.md](../docs/en/ops/repo-policy.md) (JA: [docs/ja/ops/repo-policy.md](../docs/ja/ops/repo-policy.md)). The active ruleset is `main-strict` (id `15581036`). The Owner has Admin-role bypass for emergency direct push; everyone else must go through PR.

> Build, test, lint, and release commands are exposed as Makefile targets at the project root. Run `make help` for the full list, or open [Makefile](../Makefile) / [README.md](../README.md) `### Development` section. Pre-push invariant chain: `make ci-local`. Local IPA verification via fastlane: `make release-ipa-local`.

0. **Failure Triage Discipline (when an unexpected failure surfaces)**: When a build / test / E2E / runtime failure is unexpected and cannot be reproduced by your isolated change scope (revert your edits → still fails; or fresh checkout of `main` also fails), the suspect is an upstream library / SDK / tool, not Skeinly code. **Before classifying the failure as a "CI Known Limitation" or applying any workaround**, search the upstream issue tracker — this is mandatory pre-classification, not optional. The full DETECT → RECORD → TRACK three-stage workflow + entry format + workaround expiry discipline lives in [.claude/docs/process/upstream-issue-triage.md](docs/process/upstream-issue-triage.md). Skipping this step accumulates "Known Limitations" entries that drift from upstream reality and outlive their fixes; the 2026-05-06 Maestro `127.0.0.1:7001` triage was the prompting incident — four upstream issues directly described our symptom.
1. **Issue Analysis**: Feed issues to the orchestrator via GitHub MCP or copy-paste
2. **Product Planning**: The product-manager creates PRD, user stories, and acceptance criteria
3. **Research & Reuse**: Search GitHub, package registries, and docs before writing new code
4. **Architecture**: The architect designs the solution; significant decisions are recorded as ADRs in `docs/en/adr/`
5. **Implementation**: The implementer writes code following TDD (RED -> GREEN -> IMPROVE)
   - Shared logic in `shared/src/commonMain/`
   - Android UI in `androidApp/`
   - iOS UI in `iosApp/`
6. **Quality Gate**: The code-reviewer, linter, security-reviewer, and performance-engineer validate the implementation
   - After task completion, request a code review from the code-reviewer agent
   - Fix ALL reported issues (CRITICAL, HIGH, MEDIUM, and LOW)
   - Re-run the code review after fixes
   - Repeat the review-fix cycle until no issues remain (APPROVED)
   - Only then proceed to commit
7. **Documentation**: The technical-writer updates docs and changelog. **When the change touches a feature surface that has a spec under [`.claude/docs/spec/`](docs/spec/)** (e.g. chart-editor, suggestion-flow, collaboration-history), update the spec in the **same commit** so the "current shape" view stays accurate. Specs describe *what is* — defer *why decisions were made* to the relevant ADR.
8. **Release**: The devops-engineer manages deployment and release
8.5. **Final pre-push re-verification (after ALL edits)**: Any edit made AFTER the invariant chain passed — including code-review fix-ups, ktlintFormat post-edits, single-line import reorders, default-arg additions, KDoc fixes — invalidates the prior `make ci-local` run. Before `git commit`, re-run the FULL invariant chain (`make ci-local` from project root) regardless of how trivial the last edit looked. **`make ci-local` is the single canonical pre-push entry point** — earlier session-only `make verify-all` / `make verify-ios` aliases were folded back in on 2026-05-02 after they shipped a runtime gap (XCUITest assertion failures + Maestro flow regressions) for three consecutive CI failures.

   `make ci-local` runs the following layered chain:
   - `:shared:ktlintCheck` (KMP shared module style)
   - `:androidApp:ktlintCheck` (Android-app module style — `MainActivity` and adjacent files)
   - `:shared:compileTestKotlinIosSimulatorArm64` (iOS simulator compile-test, catches Kotlin/Native test-name restrictions + commonTest type errors)
   - `:shared:testAndroidHostTest` (full shared test suite on JVM)
   - `:shared:koverVerify` (coverage gate)
   - `verifyI18nKeys` (CMP en/ja + iOS xcstrings parity)
   - `make ios-build` (iOS app `xcodebuild build` with `generic/platform=iOS Simulator` — multi-arch arm64+x86_64; catches Swift compile/link errors the specific-sim test-build path won't hit)
   - `make ios-test` (iOS XCUITest run — `xcodebuild test`, which internally does `build-for-testing` first, catches `Core/Bridging/` test-target compile regressions AND runtime XCUITest assertion failures like the Phase 39.4 4-page onboarding regression)
   - `make e2e-android` (Android Maestro suite — `bash e2e/run-android.sh`; builds debug APK + verifies running emulator + installs + runs every flow under `e2e/flows/android/` excluding `requires-supabase`)
   - `make e2e-ios` (iOS Maestro suite — `bash e2e/run-ios.sh`; same shape, excludes `skip-ios26` + `requires-supabase`)

   **Time cost ~30-45 min**, requires booted Android emulator + iOS Simulator before invocation. The chain trades pre-push wall-clock for "no daylight between local green and CI green" — it reproduces every check CI runs. Treat `make ci-local` as the single source of truth for "is this safe to push".

   Ground rules:
   - Don't trust an `Edit` operation that "only changes one line" — single-line import reorders have caused CI failures (Phase 39.5 `kotlin.time.Instant` placement).
   - Don't trust that a compile-pass implies a lint-pass. ktlint and the compiler have orthogonal rules (Phase 39.5 `import-ordering` violation passed compile).
   - Don't trust that `xcodebuild build` (app target) implies test-target builds. The `iosAppTests` target deliberately excludes `Core/Bridging/` because it doesn't link `Shared.framework`; an iOS `Shared`-importing file misplaced under `Core/` will pass app build and fail test build (Phase 39.3 regression).
   - **Don't trust that compile-pass implies test-pass.** XCUITest assertion regressions (e.g. Phase 39.4 onboarding adding a 4th page; the test still compiled because `XCTAssertTrue(getStartedButton.waitForExistence(timeout: 3))` is syntactically valid — only the runtime existence check failed) and Maestro flow-element regressions surface only at test execution. `make ci-local` runs both; do not skip either by invoking individual targets in lieu of the full chain.
   - When in doubt, run `make ci-local` one more time after the last edit and before `git commit`. The cost of a clean cache hit is small (Gradle UP-TO-DATE + xcodebuild incremental) compared to the cost of a CI red and fix-forward commit + reviewer attention.

9. **Commit, Push & CI Verification**: When the code review is APPROVED and all tests pass:
   - **Pre-commit CI check**: If any CI workflow file (`.github/workflows/`) was modified, reproduce its build/test commands locally before committing to catch task name mismatches, missing dependencies, or configuration errors early
   - **Pre-commit CI warning check**: After pushing, review CI logs for warnings (not just failures). Warnings such as deprecated actions, missing configuration fields, or fallback behaviors indicate latent issues that should be fixed proactively
   - Commit using conventional commits format (feat, fix, refactor, docs, test, chore, perf, ci)
   - Push to `origin/main`
   - **Temporary CI-wait policy (in effect from 2026-04-22, revisit when the i18n screen sweep + Phase 32.x settle)**: do **NOT** block the session waiting for CI / Security / E2E to turn green. Push and proceed directly to the next task. Rationale: each commit triggers ~30 min of remote CI, and blocking on three parallel runs per commit burns development throughput while the pre-push invariants (`ktlintCheck`, `compileTestKotlinIosSimulatorArm64`, `testAndroidHostTest`, `koverVerify`, `verifyI18nKeys`) are reliably catching regressions locally. If a prior commit's CI went red, the user surfaces it at the start of the next session as part of the handoff restate. Pre-commit CI-file reproduction and the post-push warning scan above still apply on commits that touch `.github/workflows/` — the only change is "skip the blocking CI wait".
     - **Do NOT propose `/schedule` background agents to verify CI status.** The next-session handoff is the canonical CI-verification mechanism: it instructs the resuming session to run `gh run list -L 3 --workflow=…` for each pending commit before doing anything else, fix any red, and proceed once green. Adding a scheduled-agent layer is redundant and creates a second source of truth for "did CI pass?" — the handoff already covers it. This rule applies whether the just-pushed commits are 1 or 5; the handoff lists every unverified SHA. (Rule confirmed by user 2026-04-30.)
   - These steps are performed autonomously — no user confirmation needed
10. **Next Step Planning**: At each task boundary (phase completion, milestone), convene the agent team (architect, product-manager, implementer, etc.) to discuss and agree on the next priority. Output the result as Next Session Instructions for handoff.
    - **Default to agent-team deliberation for ALL project decisions** — path choice, scope questions, prioritization, tradeoff calls. Never present a single-voice recommendation as the default when multiple reasonable paths exist. Concise roleplayed voices (product-manager, architect, implementer, knitter, ui-ux-designer as relevant; 2–3 sentences each) in a single response are acceptable for path-choice decisions; spawn real sub-agents only when the question needs deep investigation (codebase exploration, security review, etc.). End every team deliberation with an explicit **Decision:** line so the user sees the conclusion, not just the debate.
    - **Ignore implementation cost in path choices**: AI agents do the implementation, so effort/time is not a scarce resource from the user's perspective. Never cite "hours", "fits in this session", "quick win", "low-cost", or any time-estimate framing as a reason to prefer one path over another. Compare paths on user value, strategic leverage, risk, and information-gathering value only. If a path has genuinely larger scope, name the scope ("requires X new screens" / "touches Y subsystems") without a time estimate. Let the user weigh scope against value; don't pre-collapse the tradeoff by biasing toward short-horizon work. **Always pick the best outcome, not the cheapest path.**

## CI Known Limitations

> **Format discipline (per [docs/process/upstream-issue-triage.md](docs/process/upstream-issue-triage.md))**: every new entry below MUST link to its upstream tracking issue, carry a `last-checked` date, name the applied workaround if any, and set a `Re-check by` date (default monthly). Workarounds are explicitly temporary — when upstream closes the issue, the workaround commit removes itself. Entries that predate this discipline (CodeQL java-kotlin, CMP resources, iOS NavigationStack edge-swipe, XCUITest flake) are kept as-is and would be re-formatted only if their underlying issue resurfaces. New entries: use the [template in upstream-issue-triage.md §3](docs/process/upstream-issue-triage.md#3-track--record-the-entry--workaround-discipline).

- **CodeQL java-kotlin**: ✅ **Resolved twice** — first on 2026-05-02 (`1298b4b`) by switching to `:shared:assembleAndroidMain :androidApp:assembleDebug` over `:shared:compileAndroidMain` (which is a lifecycle task with no kotlinc invocation under `com.android.kotlin.multiplatform.library`). Then re-failed once Phase 39.3 had warmed the gradle-actions cache, because `:shared:compileAndroidMain` and `:androidApp:compileDebugKotlin` started returning FROM-CACHE — kotlinc never executed and the CodeQL extractor saw an empty database ("CodeQL detected code written in Java/Kotlin but this run didn't build any of it"). Second resolution adds `--no-build-cache` to the Build (java-kotlin) gradle invocation in `security.yml` so kotlinc is forced to run on every CodeQL workflow run regardless of cache warmth. `CodeQL Analysis (java-kotlin, ubuntu-latest, manual)` is in `required_status_checks` on the `main-strict` ruleset; `continue-on-error: true` removed. Both load-bearing pieces (assemble pipeline + `--no-build-cache`) documented inline in `security.yml`. The `github/codeql-action` floating `@v4` tag preserved so dependabot tracks CodeQL CLI patches.
- **Maestro 2.5.x iOS driver setup broken on Xcode 26.x**: host `make e2e-ios` fails with `Failed to connect to /127.0.0.1:7001` immediately after Skeinly app reinstall. Root cause: Maestro 2.5.x's CLI JAR omits `MaestroDriverLib/**` from its packaging include list, so the iOS driver framework's `Info.plist` is missing at extract time and `xcodebuild` aborts. The driver app never lands in `xcrun simctl listapps` and there is no listener on port 7001.
  - **Upstream**: [mobile-dev-inc/Maestro#3218](https://github.com/mobile-dev-inc/Maestro/issues/3218) (OPEN, last-checked 2026-05-06) — packaging bug with a community-supplied one-line fix in `maestro-cli/build.gradle.kts` (no PR yet). Related: [#3248](https://github.com/mobile-dev-inc/Maestro/issues/3248) (OPEN — Xcode 26.4 simulator path broken in Maestro 2.5.1, `list-devices` empty and `test --udid` hangs), [#3254](https://github.com/mobile-dev-inc/Maestro/issues/3254) (OPEN — driver becomes unresponsive after ~3 tests on iOS 26.4), [#3137](https://github.com/mobile-dev-inc/Maestro/issues/3137) (CLOSED 2026-04-07 — reporter confirmed Maestro 2.0.10 is the only version that runs on iOS 26 + Apple Silicon + Xcode 26.x; 2.3.0+ regressions).
  - **Affected scope**: host `make e2e-ios` on Xcode 26.x + Maestro 2.5.x + iPhone 17/iOS 26.4 simulator. CI runner (`macos-latest` GitHub Actions = Xcode 16-series + iOS 18 sim + iPhone 16) is unaffected — the same flow batch passes there, and `make e2e-ios` is gated server-side via the `main-strict` ruleset.
  - **Workaround**: none applied. Host iOS E2E is grey while CI gates the same suite. Maestro 2.0.10 downgrade (per #3137) reportedly works but loses 2.x features and reverses our position as an active upstream consumer; deferred per user direction 2026-05-06 ("active library, wait for the fix").
  - **Expected fix**: no upstream eta. The #3218 packaging fix is one-line and could land in any 2.5.x patch; #3248 / #3254 are deeper. Watch for a 2.5.2+ release.
  - **Re-check by**: 2026-06-06 (monthly). Re-run `gh issue view 3218 --repo mobile-dev-inc/Maestro` + try `make e2e-ios` against current host environment, update this entry's dates accordingly.
- **Maestro + SwiftUI Button rendering on iOS 26 (separate symptom from above)**: Maestro 2.4.x cannot reliably tap SwiftUI `Button` elements inside `List` sections on iOS 26 — the first tap works but subsequent taps are silently dropped. Also, `inputText` into `.searchable` does not update the SwiftUI `@State` binding. Distinct upstream surface from the driver-install issue above.
  - **Upstream**: no single canonical issue exists for this symptom cluster. Closely-related anchors that are runnable (`gh issue view <num> --repo mobile-dev-inc/Maestro`) at re-check time: [#3148](https://github.com/mobile-dev-inc/Maestro/issues/3148) (CLOSED 2026-04-28, "tapOn ... reports COMPLETED but doesn't navigate (iOS 26.2)" — same shape: tap registers but UI does not respond), [#3090](https://github.com/mobile-dev-inc/Maestro/issues/3090) (CLOSED 2026-04-04, "tapOn fails to open SwiftUI Menu when placed in a ZStack overlay" — tangential SwiftUI tap surface bug). At re-check time, also re-search with keywords `"List section"` / `searchable` / `tapOn SwiftUI iOS 26` against the Maestro repo to catch any new dedicated issue that surfaced; this entry's anchors will be promoted then.
  - **Affected scope**: flows tagged `skip-ios26` (search + filter chip variants) — already excluded from `run-ios.sh` via `--exclude-tags skip-ios26`. CI XCUITest covers the same surface natively for the same screens.
  - **Workaround**: `--exclude-tags skip-ios26` already wired in `e2e/run-ios.sh`. Per-flow tag annotations remain on the affected YAMLs.
  - **Expected fix**: rolls in with broader Maestro upstream iOS 26 support — same fix wave as #3218 / #3248 cluster.
  - **Re-check by**: 2026-06-06 (re-evaluate alongside the cluster above; if the cluster resolves, retry these flows without the tag).
- **Maestro + SwiftUI Button on headless CI**: Maestro `tapOn` (both text and accessibility ID) does not reliably register SwiftUI `Button` taps on headless CI simulators (macOS GitHub Actions runner). The iOS onboarding helper bypasses this by relaunching the app with `-has_seen_onboarding true` launch argument, which NSUserDefaults picks up from the argument domain.
- **E2E local-only mode**: CI builds use empty Supabase credentials (local-only mode). Flows that require a live Supabase backend (e.g., Discovery browse) are tagged `requires-supabase` and excluded from CI. Run these flows locally against a Supabase-connected build.
- **iOS NavigationStack edge-swipe**: iOS 16+ `NavigationStack` does not expose a public API to disable the left-edge swipe-to-pop gesture while keeping the rest of the bar interactive. `.interactiveDismissDisabled` applies only to modally presented sheets, not pushed stack entries. Screens with discard-guard semantics (e.g. the structured chart editor) hide and replace the default back button so tapping it routes through a confirmation dialog, but the edge-swipe still pops silently. Disabling the gesture requires dropping to UIKit (`navigationController.interactivePopGestureRecognizer.delegate = self`), which is out of scope for the shared-module architecture. Users on iOS who care about in-flight edits should use the top-bar back button.
- **CMP resources on AGP 9.x KMP library plugin**: The `com.android.kotlin.multiplatform.library` plugin used by `:shared` (Phase 15.5) does not wire the Compose Multiplatform resource generator into the Android assets pipeline. The `preparedResources/commonMain/composeResources/` CVR files never get copied into `assembledResources/androidMain/`, so the final APK contains zero `composeResources/` entries and any `stringResource(Res.string.*)` throws `MissingResourceException` at runtime. Worked around in `androidApp/build.gradle.kts` via a `CopyComposeResourcesForAndroid` task wired through `androidComponents.onVariants { variant.sources.assets?.addGeneratedSourceDirectory(...) }` — resolves to `assets/composeResources/<package>/values*/*.cvr` in the APK. Remove the workaround when upstream JetBrains integration lands for this plugin. Note: Phase 33.1 shipped with this crash latent (ChartEditorScreen is never opened in E2E); Phase 33.1.1 exposed it via the SymbolGalleryScreen migration, which is exercised by `e2e/flows/android/P1_symbol_gallery.yaml`.
- **iOS XCUITest `testDeepLink_invalidURL_doesNotNavigate` simulator flake**: `DeepLinkUITests.testDeepLink_invalidURL_doesNotNavigate` has flaked once on the `macos-15-arm64` GHA runner (CI run 24885305441, commit 58395e4) with `Failed to get background assertion for target app with pid NNN: Timed out while acquiring background assertion.` This is the XCTest ⇄ SpringBoard assertion-acquisition subsystem timing out — unrelated to the app code, and reproduces as transient on cold simulators under runner contention. Does not indicate a Phase 34 regression (shared + Android jobs green on the same commit). Re-run of the same workflow would almost certainly pass. If the flake becomes chronic, the two interventions are: (1) add a retry wrapper around the XCUITest invocation at the workflow level, or (2) add `CocoaLumberjack`-style debug logging to `AppRootView` to observe the foreground-activation lifecycle the assertion acquisition is gating on. Until either trigger fires, we document and tolerate.
- **iOS Maestro `P0_create_project` simulator-restart flake**: `P0_create_project` failed once on the `macos-15-arm64` GHA runner (CI run 25600433802 attempt 1, commit 48ab4fd) with `App crashed or stopped while executing flow` — the app launch reported `COMPLETED` then the XCTestDriver `viewHierarchy` probe 583 ms later returned 500 `Application io.github.b150005.skeinly is not running`. Re-run of the same workflow on the same commit (same CI run, attempt 2) passed all 5/5 iOS flows including `P0_create_project`. Pattern: only the third iOS flow in the alphabetical run order (Maestro 2.4.0 sequence: `P0_row_counter` → `P2_navigation` → `P0_create_project`) was affected; the prior two and the subsequent two flows passed end-to-end. Each Maestro flow on iOS triggers a `clearState` cycle that uninstalls + reinstalls the app + cycles `applesimutils --setPermissions all=unset` then `all=allow` (calendar / camera / contacts / faceid / homekit / medialibrary / microphone / motion / photos / reminders / siri / speech / userTracking — notifications are NOT in the set, so `UNUserNotificationCenter` stays `notDetermined`); after ~3 successive cycles the simulator's launch services daemon (`lsd`) appears to lag, allowing `simctl launch` to return `COMPLETED` while the launched process is in fact terminating. The crash window (~600 ms) is too short for any user-visible app-lifecycle code to surface; SwiftUI scene-graph init never paints (the artifact screenshot shows a blank white screen with only the status bar). Not a Phase 24.2e regression — `@UIApplicationDelegateAdaptor(AppDelegate.self)` + `aps-environment = production` entitlement were inspected and ruled out (the same code path runs in all 5 flows; reproducibility is third-flow-specific, not code-specific). Does not indicate a Phase 24.3 regression either (the failed flow doesn't touch any push code path). If the flake becomes chronic, two interventions: (1) add a retry wrapper around the iOS Maestro step at the workflow level (mirrors the proposed XCUITest retry above), or (2) explicitly `xcrun simctl shutdown <UDID>` + `boot` between iOS Maestro flows so each flow gets a freshly-booted simulator (5–10 s overhead per flow × 5 flows = 25–50 s total cost on the iOS E2E job; acceptable if needed). Until either trigger fires, we document and tolerate.
- **Security / CodeQL `swift, macos-26` `actool` 6h hang (first occurrence, transient)**: the `CodeQL Analysis (swift, macos-26, manual)` job in Security workflow run [25979870162](https://github.com/b150005/skeinly/actions/runs/25979870162) (job 76366528649, commit `29eb26d` = A33) hit the GitHub Actions 6h0m0s max-execution-time limit and was force-cancelled (`CodeQL job status was configuration error`; orphan `actool` pid 70689 + `xcodebuild` pid 25560 terminated at cleanup). Build-step timeline: `xcodebuild build` started `T03:17:39`, normal Swift compile through `T03:40:40`, then `CompileAssetCatalogVariant` → `actool … iosApp/iosApp/Assets.xcassets --compile … --compress-pngs --enable-on-demand-resources YES --filter-for-thinning-device-configuration iPhone18,1 --filter-for-device-os-version 26.4.1` started `T03:42:09` and emitted **zero output for the next ~5.5 h** until the `T09:08:43` forced cancel — a hard `actool` hang under Xcode 26.2's asset-catalog compiler on the GitHub `macos-26` runner image.
  - **Not an A33 regression** (ruled out, multi-signal): (1) A33 did **not** touch `iosApp/iosApp/Assets.xcassets` (the AppIcon assets are a closed prior item, 2026-05-08 `43a472c`/`fb0ea68`); the Swift sources — incl. the new `OssLicensesView.swift` — compiled to completion *before* the asset-catalog phase. (2) The **same commit `29eb26d`** `CI / iOS (Build + Test)` job (run [25979870135](https://github.com/b150005/skeinly/actions/runs/25979870135)) passed in 29m23s — it runs the identical `xcodebuild` with a specific-UDID destination → identical `actool --compress-pngs` on the same `Assets.xcassets`, and did **not** hang. (3) The **prior commit A20 `66c247f`** `CodeQL Analysis (swift, macos-26, manual)` (job 76309530460) passed in 44m33s on the *unchanged* security.yml — same `-destination "id=$UDID"` actool path, same CodeQL build-mode `manual` process tracer. Same code path: A20 ✓ 44m33s, A33 ✗ 5.5h ⇒ **intermittent**, not deterministic. (4) Same A33 push, `E2E Tests` (also a macOS runner running iOS Maestro) passed in 26m40s — the macOS runner pool was not globally degraded; a single-runner `actool` stall is the textbook transient signature. The CodeQL build tracer is the *most-exposed surface* (longest ~40–53min macOS job + ptrace overhead = widest window to land on a contended/bad runner), not a proven deterministic cause.
  - **Upstream**: searched `github/codeql-action`, `github/codeql`, `actions/runner-images` for `actool` / `swift extractor` / `xcodebuild hang` / `macos-26 Xcode 26.2` (2026-05-17) — **no matching tracked issue**. Consistent with a runner-side infra/contention flake (possibly coincident GitHub Actions instability) rather than a tracked upstream defect. Re-search at chronic-trigger time.
  - **Affected scope**: `Security` workflow `swift, macos-26` job only. NON-blocking in practice: development is direct-push with Owner Admin-role bypass (no PR gate), AND the `main-strict` ruleset's required swift context is `CodeQL Analysis (swift, macos-latest, manual)` while the pinned workflow emits `…(swift, macos-26, manual)` — see the operator-flag note below; the required swift check is structurally never reported regardless of this hang. `java-kotlin` CodeQL (the load-bearing security gate) passed in 7m19s on the same run.
  - **Workaround**: none applied (first occurrence — project precedent for one-off macOS-toolchain flakes, e.g. the `testDeepLink_invalidURL` XCUITest + `P0_create_project` Maestro entries above, is document-and-tolerate, not a speculative workaround commit). The next push's `swift, macos-26` run is the free natural re-exercise. **If it hangs again on an unrelated commit (chronic)**, escalate with one of: (1) wrap the `Build iOS app (swift)` step in `timeout <N>m` so a stuck `actool` is reaped and the job fails fast instead of burning 6h of macos-26 minutes; (2) add a workflow-level retry on the swift CodeQL job (mirrors the proposed XCUITest/Maestro retry above); (3) switch the swift CodeQL build to `-destination 'generic/platform=iOS Simulator'` (matches `make ios-build`; skips per-device asset thinning so `actool` runs the non-`--compress-pngs`/non-thinning path, sidestepping the hang surface) — verify CodeQL still gets a complete trace under generic-destination first.
  - **Re-check by**: 2026-06-17 (monthly), OR immediately if a future push's `swift, macos-26` job hangs again — whichever first. Re-run the upstream search + decide an escalation intervention if chronic.
  - **Operator-flag (separate, NOT transient — latent governance drift)**: `main-strict` `required_status_checks` lists `CodeQL Analysis (swift, macos-latest, manual)` but `security.yml` is pinned `runner: macos-26`, so the emitted job is `CodeQL Analysis (swift, macos-26, manual)`. The required `macos-latest` swift context is therefore **never reported** — harmless under the current direct-push + Admin-bypass model, but if the repo ever moves to PR-gated merges the swift CodeQL required check becomes permanently unsatisfiable. Fix is operator/governance (blast radius on the required-check contract): either update the ruleset's swift `required_status_checks` context to `…(swift, macos-26, manual)`, or revert the workflow pin to `macos-latest` if/when that runner alias ships Xcode 26+. Flagged for operator decision; not unilaterally edited.

## Testing Requirements

- Minimum 80% test coverage for shared module
- **Unit tests** (commonTest): Domain models, use cases, repository logic
- **Android tests**: Compose UI tests, ViewModel tests
- **iOS tests**: XCTest for SwiftUI views and ViewModels
- **Integration tests**: SQLDelight database operations, Ktor API calls
- **E2E tests**: Critical user flows (create pattern, track progress, share)

### Testing Tools

| Platform | Framework |
|----------|-----------|
| Shared (commonTest) | kotlin.test |
| Android | JUnit 5, Compose UI Testing, Turbine (Flow testing) |
| iOS | XCTest, Swift Testing |
| E2E | Maestro (YAML flows, `e2e/` directory) |

## Code Quality Standards

- Functions: < 50 lines
- Files: 200-400 lines typical, 800 max
- Validate all inputs at system boundaries
- Handle errors explicitly at every level
- Never hardcode secrets; use environment variables or BuildConfig/Info.plist
- Use `ktlint` for Kotlin formatting
- Use `SwiftFormat` / `SwiftLint` for Swift formatting
- Gradle: use version catalogs (`libs.versions.toml`)

## Output Quality Standard

**There is no token-consumption ceiling**. Output budget is not a scarce resource on this project — the user has explicitly removed it as a constraint. Optimize relentlessly for **precision and quality of deliverables** over output brevity:

- **Investigate to the bottom of the question, not to the edge of comfort.** When code review flags a finding, read every relevant file before responding. When ADR / spec writing requires understanding the current shape, dispatch a `code-explorer` agent rather than guess. When user-visible UI is at stake, exhaustively enumerate the finding categories rather than listing the easy two.
- **Never skip the agent-team deliberation step to save tokens.** Multi-voice deliberation (product-manager, architect, implementer, knitter, ui-ux-designer, security-reviewer, etc.) catches scope-cut decisions and security holes that a single-voice answer drops. The cost of running 3 voices in one response is negligible vs. the cost of shipping a wrong design.
- **Never trade test coverage for output brevity.** When a slice introduces N units of behavior, ship N tests. Don't write 4 tests "to keep the diff small" when 12 tests are needed to lock down the regression surface.
- **Don't pre-collapse documentation.** ADRs that capture every decision + rationale (1000+ lines acceptable) are net-better than 200-line ADRs that save tokens by hand-waving the tricky parts. The doc is read once during writing and referenced N times during implementation.
- **Code-review iterations are not a cost.** Land all findings (CRITICAL → LOW) before commit per the existing Workflow step 6 rule. Two review passes is the floor when any non-trivial finding surfaces.
- **Prefer doing too much over doing too little.** When in doubt about whether a check, test, or doc clarification is necessary, do it. The user explicitly chose the budget configuration that makes this safe.
- **Never trust a single WebFetch summary on identifier-precision claims.** WebFetch returns model-summarized content; the model can hallucinate exact strings (runner labels, SDK version numbers, API endpoint paths, function/class names, environment variable spellings) that are not actually present in the source page. Before reflecting any such identifier into code, config, or documentation, invoke the **verification-layer (research domain)** skill: `.claude/skills/verification-layer/SKILL.md` — Generator (`docs-researcher`) declares a Tier (T1/T2/T3) and citation list, Critic (`research-critic`) re-verifies using a different tool family from `.claude/skills/verification-layer/research/checklist.md` §"Primary-source allowlist". The full GAN-style protocol with bounded iteration lives in [`.claude/skills/verification-layer/research/protocol.md`](skills/verification-layer/research/protocol.md). The 2026-05-09 incident where `macos-26-arm64` was committed based on a single WebFetch (the canonical label is `macos-26`) cost ~2h CI queue time and a forced fix-forward commit (`545c7e9`) — that's the canonical anti-example the skill exists to prevent.

This standard does NOT override:
- The "minimal-diff change" principle in build-error-resolver mode (still scope-bounded).
- The agent-team-deliberation requirement before path choices (still mandatory).
- The CI-wait policy (still skip blocking on remote CI per the existing rule).

What it DOES override: any tendency to truncate analysis, skip clarifying questions, or collapse multi-voice deliberation into a single fast answer just because output volume is starting to feel large. Output volume is **not** the constraint here.

## Documentation Convention

- Technology reference docs: `docs/en/` (English, source of truth)
- Japanese translations: `docs/ja/` (maintained translations)
- Claude reads `docs/en/` only to minimize context window usage
- Japanese files include a header linking to the English source
- ADRs in `docs/en/adr/` and `docs/ja/adr/`

## Session Handoff Protocol

Each session MUST end with a **Session Task Prompt** block output as a fenced code block in the conversation (never written to any file). This block is the prompt the user will paste back at the start of the next session to instruct you on what to do — so write it in the imperative second-person voice ("Your task this session is X", "Run Y", "Open Z"), NOT as a third-person prediction of what a future session will do. The reader of this block IS the session doing the work.

The handoff is a short tactical prompt for resuming work — NOT a comprehensive status dump. Persistent facts live in this CLAUDE.md; the handoff only carries what the resuming session needs to act immediately.

### What goes in the handoff — keep terse (target ≤40 lines)
1. **Heading**: start with a clear task-prompt header, e.g. `# Your task this session` or `## Task for this session` — NOT `## Next Session Instructions` (that phrasing makes the reader think the task belongs to someone else).
2. **Current state**: branch + latest commit hash + test count + CI status (one line each)
3. **Your task**: one-line imperative statement of the phase / option to execute ("Implement Phase 30.4 glyph bundle", not "Phase 30.4 glyph bundle is next")
4. **First actions**: 3–5 concrete commands or edits to run immediately (e.g. `gh run list ...` sanity check, path to the first file to open, the test to write first) — phrased as direct instructions
5. **Any pending execution decision blocking completion** (merge conflict, flaky test awaiting investigation, etc.)

### What does NOT go in the handoff — put in CLAUDE.md instead
- **Roadmap progress** → `## Development Roadmap > Completed`. Move each finished phase here in the same commit that ships it. Include test delta, key design points, scope cuts.
- **Scope decisions for the completed phase** → keep in the phase's Completed bullet (what was cut, why, when to revisit).
- **Tech debt and follow-ups** → `## Tech Debt Backlog`. One line per item, grouped by theme.
- **Architecture invariants, ADR references** → `## Architecture Principles` or the relevant ADR under `docs/en/adr/`.
- **Gotchas / local-CI divergence** → `## CI Known Limitations`.

If the handoff is growing past ~40 lines it means you are duplicating CLAUDE.md — move the content there and link to it by phase name only.

**Ordering**: emit the handoff as the very last message of the session, after every execution decision (push, CI verify, etc.) is resolved. Never emit it mid-session — state can change afterwards and the handoff goes stale.

**Voice check**: before emitting, re-read the block as if you were opening a fresh session and someone handed it to you. If any sentence reads as "the next session will do X" rather than "do X", rewrite it. The user copy-pastes this block verbatim as the first message of the resuming session — it must read as a direct instruction to that session, not a description of it.

## Specifications

Feature-organized "current shape" docs. Loaded into agent context when extending a specific feature; cheaper than re-discovering structure via grep + reading scattered Phase entries. Specs describe *what is*; ADRs answer *why we decided that*.

| Spec | Covers | Key ADRs |
|---|---|---|
| [docs/spec/chart-editor.md](docs/spec/chart-editor.md) | Chart authoring (palette + canvas + history + save). Phase 32.x / 35.1a–d / metadata picker | ADR-007, 008, 009, 011, 013 |
| [docs/spec/suggestion-flow.md](docs/spec/suggestion-flow.md) | Suggestion open / list / detail / comment / close / apply / conflict resolution. Phase 38.x (was `pull-request-flow.md` pre-2026-05-10 terminology audit) | ADR-012, 013, 014 |
| [docs/spec/collaboration-history.md](docs/spec/collaboration-history.md) | `chart_versions` append-only spine, version history / variants / comparison / restore. Phase 37.x | ADR-007, 013 |

Specs intentionally do NOT exist for: `i18n` (script-verified parity is the source of truth), `auth` (small surface, well-trodden), `realtime-sync` / `discovery-fork` / `segment-progress` / `symbol-catalog` (sufficiently captured by their respective ADRs).

## Development Roadmap

### Archive policy

When a wave fully closes (no more sub-slices expected — typically signaled by an in-entry "closes Phase N entirely" remark + no follow-up entries for ≥1 session), promote that wave's Phase entries from [`### Completed`](#completed) to [`docs/en/phase/completed-archive.md`](../docs/en/phase/completed-archive.md) and replace them with a single-line index pointer. The same applies at major-release commits (e.g. v1.0 / Phase 40) for any wave still inline. Cap the in-CLAUDE.md `Completed` section at the **most recent two open waves** so the file stays under ~300 lines and individual edits stay grep-friendly.

Cadence: do this in a dedicated docs commit at wave-close time, OR fold it into the major-release commit (alongside `version.properties` bump) — whichever comes first. The promotion is mechanical: cut the closed-wave Phase entries, append them to the archive verbatim, replace with the index pointer.

Tech Debt items marked **CLOSED** in [`## Tech Debt Backlog`](#tech-debt-backlog) follow the same archive policy: kept inline only while the trail is referenced from a recent Phase entry; otherwise promoted to the archive's "Closed Tech Debt trails" section.

### Completed

> **Pre-Phase 38 + Phase 41.x history archived**: Phase 1 — 37.x (chart editor data spine, structured chart authoring, segment progress, Discovery + fork, collaboration core, history / branch / diff) and Phase 41.x (F1 Pro subscription + dynamic symbols, closed at 41.5 / 41.5.1) are fully shipped and stable. Full design rationale + scope cuts + code-review findings → [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md). The entries below cover the active beta wave only: Phase 38.x (PR/merge — beta validates this) + Phase 39.x (closed-beta tooling).
>
> - Phase 41.x — F1 Pro subscription + dynamic symbols (ADR-016, closed at 41.5 / 41.5.1) → archived
> - Phase 24.x — Push notifications (ADR-017 + ADR-018, closed at 24.6) → archived 2026-05-15
> - Phase 26.x — Auth infrastructure: OAuth + MFA + biometric + post-OAuth profile gate (ADR-022, closed at 26.8) → archived 2026-05-15
> - Phase 25.x — Friend-Only / Private Circle Mode (ADR-024, closed at 25.5) → archived 2026-05-16

- **Phase 24.x — Push notifications** (closed at 24.6, ADR-017 + ADR-018) — full wave detail (3 entries originally inline here: 24.2 / 24.1 / 24.0) promoted to [completed-archive.md `### Phase 24.x — Push notifications`](../docs/en/phase/completed-archive.md#phase-24x--push-notifications-closed) on 2026-05-15.

> **Phase 39.x — Closed-beta tester rubric + F2 bug-reporting** (39.0 prep / 39.0.1 infra / 39.0.2 polish Sprints A+B / 39.1 ADR / 39.2 BuildFlags / 39.3 PostHog / 39.4 consent UI / 39.5 gestures+submission): all 9 sub-slices shipped. Full content → [docs/en/phase/completed-archive.md](../../docs/en/phase/completed-archive.md#phase-39x--closed-beta-tester-rubric--f2-bug-reporting-closed).

> **Phase 38.x — Pull-request workflow** (38.0 ADR / 38.1 data spine / 38.2 list / 38.3 detail+comment / 38.4 merge+conflict resolution): all 5 sub-slices shipped. Full design rationale + scope cuts + code-review findings → [docs/en/phase/completed-archive.md](../../docs/en/phase/completed-archive.md#phase-38x--pull-request-workflow-closed).

### Deferred (superseded by ADR-007)
- **Phase 27c**: v1 Store Submission (Final) — staged but not executed. Will re-open only after the structured chart vision (Phase 29–40) reaches beta readiness.

### Planned — Structured Chart Authoring (per ADR-007)
- **Phase 34**: Per-Segment Progress — stitch/section granularity for todo/wip/done, progress visualization overlays on the chart viewer. Needs an ADR (segment table vs. layer extension).
- **Phase 35**: Chart Editor (Advanced) — symmetry copy, layer ops, snap grid, polar-coordinate (round) mode, grid size picker.
- **Phase 36**: Chart Discovery + Fork — extend Discovery to structured charts, upgrade fork to a commit-rooted copy with author attribution.
- **Phase 37**: Collaboration Core — ✅ shipped. Commit history, branch, diff view, restore. All 4 sub-slices landed (37.0 ADR / 37.1 data / 37.2 history / 37.3 diff / 37.4 branch + restore).
- **Phase 38**: Pull Request — ✅ shipped. Comment + approval workflow, merge strategies, conflict visualization. All 4 sub-slices landed (38.0 ADR / 38.1 data / 38.2 list / 38.3 detail+comment / 38.4 merge+conflict resolution).
- **Phase 39**: Closed Beta — Phase 39.0 prep ✅ shipped (commit `23c1ee1`): version baseline + `docs/en/phase/phase-39-beta-rubric.md` (~250 lines) + `.github/ISSUE_TEMPLATE/beta-bug.yml` bilingual template. **Versioning pivot (2026-05-05)**: per agent-team deliberation, the original `1.0.0-beta1` marketing version was abandoned in favor of semver `0.1.0` (major==0 = pre-stable API). Driver: iOS `CFBundleShortVersionString` accepts only digits and dots — hyphen-suffixed prerelease identifiers (`1.0.0-beta1`) are rejected by `xcrun altool` / App Store Connect at upload time. Consequence: `version.properties` VERSION_NAME = `0.1.0`, VERSION_CODE = 1 (first Play Console upload — no prior upload exists, so VERSION_CODE starts at 1); `iosApp/project.yml` MARKETING_VERSION = `0.1.0`, CURRENT_PROJECT_VERSION = 1 (fresh stream; CI overrides via `github.run_number` so the first tag-driven Release workflow run produces iOS build 1, matching Android VERSION_CODE 1 for parity); `BuildFlags.isBeta` codegen rule pivoted from `-beta` suffix detection to `(major == 0)`. Phase 40 GA bumps the major to 1. Beta launch (tag push + invites) HARD-GATE status: **(1) ✅ Migration 016 prod-applied** (verified via Supabase MCP `list_migrations` 2026-05-02; bonus: migrations 017 subscriptions / 018 feedback / 019 avatars also already on prod — confirmed again 2026-05-05 alongside the F1 41.1 sequencing decision, and 017 `subscriptions` schema confirmed compatible with ADR-016 §3.3 RPC expectations: status text + expires_at timestamptz + RevenueCat-ready columns `platform`/`product_id`/`original_transaction_id`/`is_in_trial`/`auto_renew_status`/`latest_receipt`). **(2) ✅ Latest commit CI all green** (`728af4c` CI / E2E success at 2026-05-05 sanity check; Security expected to pass per the precedent of preceding green commits). **(3) ✅ Android VERSION_CODE = 1** (reset 2026-05-09 from the interim 4 once the user confirmed there has been no prior Play Console upload — the first Internal Testing track upload will land at code 1 to match iOS `github.run_number = 1` on the first Release workflow run). **(4) ⏳ TestFlight artifact upload validation** — risk eliminated by the 2026-05-05 pivot to `0.1.0` (no hyphen suffix; CFBundleShortVersionString accepts only digits and dots, so `xcrun altool` / App Store Connect won't reject the marketing version on format grounds). User-side action at tag-push time (Apple Developer 2FA + credentials required, not autonomously executable). **(5) ✅ `.github/ISSUE_TEMPLATE/beta-bug.yml` indexed by GitHub** (auto-indexed on push of `23c1ee1`). **(6) ⏳ Tester invites** — TestFlight Internal + Play Internal Testing user-side action. Full rubric: tester profile (5–10, includes ≥1 round-chart author + ≥1 ja-JP tester), distribution (TestFlight Internal + Play Internal Testing), per-phase test rubric for 32/34/36/37/38, known-broken list (iOS Discovery thumbnail static placeholder, polar authoring deferred to 35.2, three-pane canvas preview deferred polish, iOS chart-clone-failure toast Tech Debt, ViewModel error i18n Tech Debt, iOS edge-swipe limitation), severity classes, exit criteria (≥5 testers complete Phase 32+34+38 happy paths, zero CRITICAL, ≤3 HIGH, ≥1 round-chart-author Phase 35.2 priority signal). **Pre-v1 breaking-change policy** (per `~/.claude/projects/.../memory/pre_v1_breaking_changes.md`): until Phase 40 GA, schema migrations / API refactors / version-stream resets are permitted without backward-compat shims when they produce a better v1.0 outcome. Data migration compat does NOT freeze at Phase 39 launch — beta reset of tester data is acceptable. Phase 40 GA re-imposes backward-compat discipline. Phase 35.2 polar editor sequenced post-beta close based on rubric signal. **F1 41.1 parallel work** (per (d-2) deliberation 2026-05-05): Phase 41.1 (data spine + migration 020 + 021) proceeds autonomously in parallel with Phase 39 launch readiness; safe because Phase 41.1 ships net-new tables only (`symbol_packs` + `symbol_pack_locales` + `user_symbol_pack_state`) with no existing-table column changes, and migration 017 `subscriptions` is already prod-applied + ADR-compatible.
- **Phase 40**: v1.0 Public Release — re-open the Phase 27c submission work with the finalized product name and bundle ID.

### Residual task inventory

User-facing terminology aside, the items below are the only work scoped against three release horizons. **Currently active = post-beta only** (Phase 39 closed beta is the active milestone).

#### post-alpha (= pre-Phase-39 launch, currently in scope)

**Zero items.** All Phase 39 launch HARD-GATE items 1-3 + 5 are ✅; 4 (TestFlight upload validation) and 6 (tester invites) are tag-push-gated.

#### post-beta (= post-Phase-39 close, pre-Phase-40 GA)

| Item | Category | Notes |
|---|---|---|
| **Terminology audit (full app, EN + JA)** | UX research / i18n | Comprehensive sweep of every user-facing word for layperson-knitter comprehension; rename code identifiers + retranslate JA where needed. **Trigger = audit-target stability, NOT a calendar/beta gate** (operator directive 2026-05-15): start once implementation has progressed to a state where user-facing terminology surfaces no longer change — doing it earlier just re-audits churning copy. It is therefore NOT strictly post-beta; this row stays in the post-beta table only because that is its earliest *plausible* stability window, not a hard gate. A resuming session should assess "have the user-facing-copy surfaces stabilised?" rather than "is beta closed?" before deferring it again. |
| **StitchBook competitive analysis (D0.1)** | UX research | market-analyst + ui-ux-designer agent協議 + competitor screenshot capture → competitive matrix |
| **ChartEditor zoom + WCAG 2.5.8 (M5)** | Accessibility | Sprint B audit deferred. ~150 LOC Compose + ~100 LOC SwiftUI. Reopen if real beta-tester feedback signals it. |
| **`rememberSwipeToDismissBoxState` outer `@OptIn` cleanup** | Deprecated API | Inner partial-cleanup done; outer `TopAppBar`-using screens still need `@OptIn(ExperimentalMaterial3Api::class)` until CMP minor stabilizes TopAppBar. |
| **`jis.crochet.reverse-sc` chevron form** | Cosmetic polish | Awaiting real beta-tester feedback before redesign. |
| **CLAUDE.md slimming (≤300-line target)** | Doc hygiene | Wave-close promotion cadence; Phase 40 GA major-release commit. |
| **iOS NavigationStack edge-swipe abuse** | UX limitation | Requires UIKit drop; out of shared-Compose scope. |
| **ViewModel error i18n complete sweep** | Tech Debt | ✅ **CLOSED 2026-05-15** (commit `b8604f9`). Post-Phase-G.1, only PackManagementViewModel + PaywallViewModel still emitted hardcoded EN error strings to UI state (iOS rendered them verbatim). Pack → `ErrorMessage.LoadFailed` (reuses `error_load`); Paywall → new screen-specific `PaywallError` enum (BugReport `ErrorKind` precedent) + 2 new i18n keys (`state_paywall_package_unavailable`, `state_restore_failed`) × en/ja CMP + iOS xcstrings. Compose `.localized()` / SwiftUI `.localizedString` / `paywallErrorKey(for:)`. Tests retargeted from raw-string-leak to typed-contract assertions (all 8 sites). Verified: gradle invariant block + ios-build + e2e-android 9/9 green; ios-test/e2e-ios CI-gated (Xcode 26.x local limitation). |

#### post-release (= Phase 40 GA shipped, v1.0 public)

| Item | Category | Notes |
|---|---|---|
| **Phase 21: macOS Target** | Platform expansion | KMP shared + SwiftUI macOS app. Post-v1.0 user demand-driven. |
| **Foundation stitches (fsc, fdc)** | Crochet catalog | US-dominant convention; add when JP-pattern user reports needing them. |
| **Extended sc (exsc)** | Crochet catalog | CYC-standard, JIS silent. |
| **Spike stitches (long-loop variants)** | Crochet catalog | Colorwork crochet. |
| **`turning-ch-N` family** | Crochet catalog | ADR-009 §9 open question (parametric vs. discrete family). |
| **`verify-receipt` Edge Function reconsideration** | Phase H' (provisional) | Post-beta if a real fail mode that RevenueCat does not catch surfaces, file new ADR. |
| **CI Known Limitations re-check cadence** | Maintenance | Maestro 2.5.x driver setup + others; monthly upstream review per docs/process/upstream-issue-triage.md. |

The `post-beta` table is the working list during Phase 39 closed-beta operation. The `post-release` table is the v1.0+ outlook and is intentionally lighter — items there land via a new ADR if reopened.

### Pre-Phase-40 polish

Items deferred to post-Phase-39-beta-close, pre-Phase-40-GA. Carry-over from the 2026-04-30 user-feedback session, residual after the active-backlog.md retire 2026-05-06.

- **AppIcon brand refresh** (**CLOSED** 2026-05-08, commits `43a472c` + `fb0ea68`): the iOS appiconset (Contents.json + 19 sized PNGs under `iosApp/iosApp/Assets.xcassets/AppIcon.appiconset/`), Android launcher (5 dpi mipmap buckets `ic_launcher{,_round,_foreground}.png` + `mipmap-anydpi-v26/ic_launcher{,_round}.xml` adaptive descriptor + `values/colors.xml` `ic_launcher_background = #EED952`), Play Store listing icon (`androidApp/src/main/ic_launcher-playstore.png`, 512×512), and web favicon set (`docs/public/{favicon-{16,32}.png,favicon.ico,apple-touch-icon.png,android-chrome-{192,512}.png,og.png,site.webmanifest}`) all swapped to the new Skeinly skein-motif design (white skein + crochet hook + knitting needle on `#EED952` yellow ground). Source assets shipped from `/Users/b150005/Development/Assets/AppIcon/Skeinly_Adaptive_AppIcon/{ios,android,web}/`. iOS Contents.json restructured from `idiom: universal` 19-slot to `idiom: iphone/ipad` 19-slot. Play Store icon reuses `web/android-chrome-512x512.png` directly (Play Console spec — 32-bit fullbleed PNG ≤1 MB — accepts the same asset; no quality loss vs a separate downscale). Web set is 1:1 referenced from `docs/public/{,ja/,privacy-policy/,terms-of-service/,ja/privacy-policy/,ja/terms-of-service/}index.html` + `site.webmanifest`'s android-chrome paths. `:androidApp:assembleDebug` and iOS `xcodebuild ... build` verified green on `43a472c`; web/Play Store assets are inert (no source code) so `fb0ea68` is asset-only.
- **StitchBook competitive analysis** (D0.1): reference [StitchBook on App Store JP](https://apps.apple.com/jp/app/stitchbook/id6757340779). Overlapping core (knitting pattern + project tracking); Skeinly differentiates via Git-like PR/branch + community + pattern editor. User noted StitchBook has features and design merits worth studying. Action: market-analyst + ui-ux-designer agent協議 + screenshot capture from StitchBook for visual reference; output competitive matrix for monetization-strategist to factor into Phase 41 RevenueCat pricing. Phase: pre-Phase-40 polish (post-Phase-39-beta).
- **A21 Option B — self-service web data-deletion Edge Function** (deferred from Phase 27.3, 2026-05-15): Phase 27.3 shipped the static mailto-fallback HTML page (per the agent-team decision documented in the Phase 27.3 entry above). ADR-023 §sub-slice plan's original prescription was a `wipe-my-data-web` Supabase Edge Function with `verify_jwt = true` for true self-service web wipe (~80 LOC Deno + ~50 LOC tests). Upgrade path when reopened: (a) build the Edge Function at `supabase/functions/wipe-my-data-web/` mirroring `submit-bug-report` / `revenuecat-webhook` structure (Deno + JWT verification + `wipe_own_data()` RPC call as the user's session), (b) ~6 Deno tests covering missing Bearer → 401 / invalid → 401 / happy → 200 / RPC error → 500 (envelope `{ ok: false, message }`) / double-call idempotency → 200/200 / non-POST → 405, (c) extend `docs/public/data-deletion/index.html` with a Supabase Auth sign-in form + phrase-typing confirmation + POST to the Edge Function. Reopen criteria: real signal from beta testers reporting friction with the mailto path (e.g. delays affecting GDPR-mandated response timelines beyond the 7-day operator commitment), or if Phase 40 GA Apple/Google review feedback flags the absence of a true self-service web flow. Until then, the mailto pattern matches `account-deletion`'s established operator workflow.
- **A20 Option B — in-app "Export My Data"** (**CLOSED** 2026-05-16): in-app GDPR Art. 20 / CCPA export shipped on Compose + SwiftUI. **Delivery decision** (implementation-time agent-team): **direct download via the OS share sheet**, NOT email — no email-send infra/secret surface, instant for the beta tester, matches the SOP's "downloadable bundle" promise; the bundle data is small (avatar *bytes* deliberately excluded — see scope cut). **Server**: `supabase/functions/export-my-data/` Edge Function (`verify_jwt = true`, config.toml registered) — `index.ts` (JWT-`sub`→`userId`, **never** from body; UUID-regex guard so the service-role `.or()`/`.eq()` filters are provably injection-free; per-user 5/hr rate limit; service-role client because the bundle needs `auth.users` + `storage.objects`) + `query.ts` (pure `DataExportPort` composer = SOP step-4 17-table enumeration + `auth.users` projection + `avatars` object **metadata** + static `notes` pointing at the step 6–8 not-held-here sources) + 20 Deno tests (`deno fmt` clean; not CI-gated — manual per repo convention) + README. **No new secrets** (auto-injected `SUPABASE_URL`/`SERVICE_ROLE_KEY` only — unlike the bug-report/UGC GitHub-App trio). **KMP**: `DataExportBundle` model + `DataExportRemoteOperations`/`RemoteDataExportDataSource` (supabase-kt `functions.invoke`, auto-attaches session JWT; envelope→typed `DataExportException`; closed-code constants) + `DataExportRepository`/Impl (offline-first: `getOrNull` remote → RequiresConnectivity, signed-out short-circuit → SignInRequired; mirrors WipeData) + `DataExportViewModel` (lambda-seam, idempotency guard, share-sheet-before-success-state ordering) + `DataExportSaver` expect/actual (Android FileProvider `${applicationId}.fileprovider` + `cache-path export/` + `ACTION_SEND`; iOS `NSData` write to temp + `UIActivityViewController` from a shared `topMostViewController()` util) + `Instant.formatDateStamp` + Koin (Repository/ViewModel/Platform). **UI**: Compose `DataExportScreen` + SwiftUI `DataExportView` (`ScopedViewModel` bridge) + Settings → **Privacy** row (below Blocked Users — non-destructive READ, NOT Danger Zone) + NavGraph/AppRouter routes + KoinHelper/ViewModelFactory bridges. **i18n**: 10 new keys × en/ja CMP + iOS xcstrings (record-count is `%d`-parameterized per the codebase param-key convention); `verifyI18nKeys` green. **Tests**: 20 Deno + DataExportRepositoryImplTest 9 + DataExportViewModelTest 6 + DateTimeFormatTest +3 (now 8). **Scope cuts**: (a) avatar **image bytes** excluded — bundle carries object metadata only (the user already holds their own uploaded image; inlining base64 would unbound the response). The operator SOP step-4 Storage download remains for users who need the binaries; the in-app `notes` documents this so the export is not silently lossy. (b) `extractUserIdFromAuthHeader` duplicated from `submit-ugc-report` (a ~20-line pure decoder) NOT shared into `_shared/` — same risk-batching rationale as the github_app.ts GA-batch note (sharing would couple a new function's deploy to the live beta function for no functional gain). Reviews: security-reviewer (IDOR invariant **HOLDS** — verified no body/param/header-other-than-Authorization influences the queried id) + code-reviewer (APPROVE) + kotlin-reviewer (APPROVE) + typescript-reviewer (2 HIGH); all CRITICAL→LOW findings landed. **SOP retained as the documented fallback** ([docs/en/ops/data-export-sop.md](../docs/en/ops/data-export-sop.md) + JA updated). **Operator-side action remaining**: `git pull origin main && supabase functions deploy export-my-data` (no new secrets); smoke-test per `supabase/functions/export-my-data/README.md` §Deploy. Until deployed, the in-app button returns a generic error and the SOP path is the only working route.
- **A33 OSS attribution — AboutLibraries automation** (**CLOSED** 2026-05-17): manual `docs/public/licenses/index.html` (39 hand-listed entries, already version-drifted — coroutines listed 1.10.2 vs actual 1.11.0) replaced by build-time generation via the `com.mikepenz.aboutlibraries.plugin` 14.2.0 Gradle plugin → **258 libraries** auto-inventoried, rendered in-app under Settings → About → Open Source Licenses (Compose `LazyColumn` on Android + native SwiftUI `List` on iOS), replacing the external-browser link. **Key decisions (deviations from the 2026-05-12 scope sketch, all agent-team-deliberated):** (1) Plugin applied to **`:androidApp`**, NOT `:shared` — `com.android.application`'s runtime classpath is the complete shipped-OSS superset (`:shared` transitive + androidApp-direct Sentry/PostHog/Firebase/Credentials/Biometric that a `:shared`-applied scan misses); also avoids depending on the brand-new v14.2.0 `com.android.kotlin.multiplatform.library` task-registration fix. Empirically verified 258/258 incl. all androidApp-only deps, so the originally-planned `config/` hand-authored sentry/posthog additions were unnecessary. (2) **`aboutlibraries-core` only** — the `-compose-m3` `LibrariesContainer`/`Libraries()` UI artifact is deliberately NOT used; a shared `OssLibraryParser` maps `Libs.Builder().withJson()` → a flat `OssLibrary` model rendered identically by Compose + SwiftUI (platform parity, no Compose-on-iOS, smaller dep surface). (3) **Generate-and-commit** the JSON at `shared/src/commonMain/composeResources/files/aboutlibraries.json` (regen: `./gradlew :androidApp:exportLibraryDefinitions`) — flows through the SAME static-composeResources pipeline as `values/strings.xml` on both platforms, sidestepping the AGP-9-KMP CMP-resource task-ordering hazard (the documented Known Limitation). `prettyPrint = true` keeps dependency-bump drift a reviewable git diff; `excludeFields = [License.licenseContent, funding]` keeps it compact. (4) `OssLicensesViewModel` uses the **lambda-seam DI** precedent (DataExport): `loadLibraries` injected, production binds the `Res.readBytes` loader (kover-excluded resource plumbing), commonTest injects fakes. `OssLibrary.uniqueId` (Maven coord, verified 258/258 unique) is the `LazyColumn` key / SwiftUI `ForEach` id. State default `isLoading = true` + a `private var inFlight` re-entry latch (so init's auto-load isn't self-guarded) → spinner from frame 1, no error/empty flash; 4-branch screen (hasError→error+Retry / isLoading→spinner / non-empty→list / else→unreachable-empty no-Retry), mirrored Compose↔SwiftUI. **i18n**: 1 new key `state_oss_licenses_load_failed` × en/ja CMP + iOS xcstrings (`verifyI18nKeys` 624→ parity green); title reuses existing `action_open_source_licenses`. **Tests**: +14 commonTest (8 `OssLibraryParserTest` mapping/sort/license-join/website-fallback + 6 `OssLicensesViewModelTest` init-load/failure/Retry/re-entry/default-loading); the `parseOssLibraries` JSON decode is intentionally NOT re-tested (delegates to aboutlibraries-core's own tested `withJson`; testing it would couple to their schema). **Reviews**: code-reviewer + kotlin-reviewer + swift-reviewer, two full passes, all CRITICAL→LOW landed; the swift-reviewer's `.trackScreen` MEDIUM was dismissed (factually-wrong premise — the analogous DataExport/BlockedUsers/Connections Settings-leaf precedent is deliberately NOT in the `Screen.kt` analytics taxonomy). Static `docs/public/licenses/` (+ JA) retained as the reviewer-facing no-install surface; footers updated to point at the in-app authoritative list. **Settings-entry rename to a general "Help & Feedback" section + analytics consideration are NOT in scope** (the row already lives in About and is not beta-gated). **Future dependency bumps**: rerun `./gradlew :androidApp:exportLibraryDefinitions` and commit the regenerated JSON (one command vs hand-editing HTML — the drift class A33 eliminates). Verified: Kotlin invariant block (ktlint ×2 / iOS-sim compile / full shared suite incl. +14 / koverVerify / verifyI18nKeys) all green; full `make ci-local` (incl. ios-build / ios-test / e2e) is the pre-push gate.
- **A16 Universal Links deploy** (scheduled pre-Phase-40 GA, 2026-05-12): The code wiring landed 2026-05-12 (`AppDelegate.application(_:continue:restorationHandler:)` + `extractUniversalLinkRoute` + AASA template at `docs/well-known/`). To activate Universal Links end-to-end, the AASA file needs to land at the **root domain** path (`https://<host>/.well-known/apple-app-site-association`) which the current Project Pages site at `b150005.github.io/skeinly/` cannot serve. See `docs/well-known/README.md` for the 3 deploy-target options (custom domain / User Pages repo / external PaaS) with recommendation = User Pages repo for alpha. User-side actions when deploying: (1) pick target + deploy AASA, (2) substitute `TEAMID_PLACEHOLDER` with the actual Apple Developer Team ID, (3) regenerate Distribution Provisioning Profile with Associated Domains capability so the entitlement is preserved at signing time.
- **A25 Reduce Motion — iOS SwiftUI sweep** (**CLOSED** 2026-05-16): Compose half shipped 2026-05-12 (`ReduceMotionDetector` expect/actual + OnboardingScreen `pagerState` gate). iOS half this sweep: new `iosApp/iosApp/Core/ReduceMotion.swift` `withMotion(reduceMotion, animation, body)` helper (`reduceMotion ? body() : withAnimation(animation) { body() }`); each affected view reads `@Environment(\.accessibilityReduceMotion)` (SwiftUI-native reactive binding to `UIAccessibility.isReduceMotionEnabled` — the SwiftUI-only counterpart of the Compose `ReduceMotionDetector`; no Kotlin bridge for pure-SwiftUI views). The 2026-05-12 "affected surfaces" estimate (banner slide / notifications row / paywall sheet) was pre-sweep — the actual sweep enumerated every `withAnimation`/animated slide `.transition` and resolved exactly the 4 in-scope files: `SuggestionDetailScreen.swift` (×4 close/merge toast), `ChartConflictResolutionScreen.swift` (×2 merge toast), `ChartImageViewer.swift` (×2 zoom — zoom stays functional, only the decorative spring bounce drops), `SettingsScreen.swift` (slide `.transition` made RM-aware → cross-dissolve, defensively forward-safe; currently inert as the toast assignment has no animation transaction). Out of scope (correctly untouched): pure `.transition(.opacity)`-only toasts with no `withAnimation` (Discovery forkResultToast / ChartViewer) — instant ⇒ already RM-safe. `audits/a11y-baseline-2026-05-12.md` row A25 fully closed (see §A25 Closure). swift-reviewer APPROVED; `make ios-build` green.
- **A1 + A5 UGC moderation full UI** (foundation **CLOSED** Wave E 2026-05-12; **client UI CLOSED** 2026-05-16 — Android `4ebecd2`, iOS parity `33e5d7c`): Foundation ship list (Wave E): Migration 031 `ugc_reports` (closed-enum state machine open → triaging → resolved_remove/resolved_keep/dismissed; `github_issue_url` back-link column + `state` audit timestamps) + `user_blocks` (composite PK + self-block CHECK + ON DELETE CASCADE on auth.users for ADR-005 deletion parity). Migration 032 amends 4 SELECT policies (patterns / comments / suggestions / suggestion_comments) with `NOT EXISTS (user_blocks WHERE blocker_id = auth.uid() AND blocked_id = <author_or_owner>)` arm — RLS-level server-side guarantee that blocked content never reaches the blocker's device. Edge Function `submit-ugc-report` (Deno + `verify_jwt = true` + Skeinly Feedback GitHub App, reuses EF-7 secrets — see ADR-021 §A1 for the rejection of merging with `submit-bug-report`): decodes JWT `sub` → `reporter_id`, validates body (closed-enum `target_type` / `reason_category` + UUID `target_id` + 1..2000-char `reason`), per-user 10/hr rate limit, INSERTs as user (RLS-enforced "Users can file own reports"), POSTs GitHub Issue with label `ugc-report` to `b150005/skeinly`, UPDATEs `github_issue_url` via service-role (best-effort; DB row is canonical when Issue POST fails — operator monitors `WHERE state = 'open' AND github_issue_url IS NULL`). 47 Deno tests pass (21 index + 26 mapping). `github_app.ts` duplicated from `submit-bug-report/` — Tech Debt: refactor to `supabase/functions/_shared/` at pre-Phase-40 GA. **Divergence analysis pre-done 2026-05-16** (so the GA session need not re-investigate): the two copies differ *cosmetically only* — `submit-bug-report/` is 4-space-indented (430 LOC), `submit-ugc-report/` is `deno fmt`-default 2-space-indented (478 LOC; the +48 is the 2-space formatter splitting the PKCS#8 ASN.1 byte array one-hex-per-line + wrapping long signatures, plus a 9-line "DUPLICATED from…" header). Exported API is byte-identical (`GithubAppCredentials` / `CreateIssueInput` / `CreateIssueResult` / `_resetCachesForTests` / `getAppJwt` / `getInstallationToken` / `createIssue`) and the logic is identical modulo whitespace — **no functional drift; the duplication hazard has not materialized into a bug**. Consolidation to `_shared/` (Supabase's canonical shared-code pattern; the designated target) is therefore mechanically safe and provable via running BOTH existing Deno test suites against the unified module. It stays **GA-scheduled, not pulled forward**, by deliberate risk-batching: `submit-bug-report` is the LIVE Phase 39 W5b beta bug-report channel; consolidating changes its deploy bundle, so the coupled redeploy of a working production EF is batched to the GA deploy window rather than executed mid-closed-beta for a non-urgent cleanup. When reopened: create `supabase/functions/_shared/github_app.ts` (adopt the 2-space `deno fmt` form), repoint both `index.ts` imports to `../_shared/github_app.ts`, run `deno test` in both function dirs, and flag the operator to redeploy BOTH `submit-bug-report` + `submit-ugc-report` together. Operator runbook `docs/en/ops/ugc-moderation-sop.md` (+ JA mirror, ~280 lines each): 24-hour SLA, 3-state resolution matrix (resolved_remove / resolved_keep / dismissed), Block User operator workflow with display_name → UUID resolution recipe, daily / weekly monitoring queries (oldest open report / Issue-mirror-failed rows / brigading detection), edge cases (deleted target rows / operator-as-tester recusal / duplicate reports / cyclic block/unblock). **GA full UI scope — CLOSED** (Android `4ebecd2` + iOS parity `33e5d7c`, both 2026-05-16): Compose shipped `ReportContentDialog` / `BlockUserConfirmDialog` (named "Block <name>?" title) / `BlockedUsersScreen` + Discovery card overflow Report + Suggestion overflow Report/Block (author) + Settings → Privacy → Blocked Users row + NavGraph route; 28 new i18n keys × en/ja CMP + iOS xcstrings (`verifyI18nKeys` green); 54 commonTest (repo 26 + 3 VMs 28). iOS: the 3 SwiftUI views (`ReportContentSheet` / `BlockUserConfirmView` / `BlockedUsersListView`) + KoinHelper bridges + `ViewModelFactory` factories + `AppRouter` route + Settings → Privacy → Blocked Users entry landed in `4ebecd2`; `33e5d7c` wired the residual entry affordances — Discovery trailing-swipe Report (`.sheet(item:)` on the List, paired with the existing leading Save-copy swipe) + SuggestionDetail ellipsis-Menu Report/Block gated on the loaded suggestion + non-null authorId, mirroring the Compose overflow. Block-entry sits at the suggestion-author level (the app has no standalone public user-profile screen); the original "Block User on user profile" scope item is satisfied by the Suggestion overflow + the Settings → Blocked Users management list. ADR-021 §D4 is fully closed on the client; only the server-deploy operator actions below remain. **User-side action at deploy time**: (1) `git pull origin main && supabase functions deploy submit-ugc-report` (no new secrets — reuses EF-7); (2) Create GitHub Issue label `ugc-report` on `b150005/skeinly` (Issues → Labels → New) — the Edge Function applies this label and GitHub returns 422 if missing; (3) Smoke test via README curl recipe; (4) Wire migration 031 + 032 SQL into the Supabase Dashboard SQL Editor "Saved queries" so the operator can paste-and-resolve faster during triage. See [ADR-021](../docs/en/adr/021-pre-alpha-ugc-moderation.md) for the design + alternatives + compliance posture matrix.

### Planned — Phase 24 Push Notifications (closed 2026-05-13)

All 7 sub-slices shipped and archived. See [completed-archive.md `### Phase 24.x — Push notifications`](../docs/en/phase/completed-archive.md#phase-24x--push-notifications-closed) for the full wave detail + operator-side actions.

### Planned — Phase 25 Friend-Only / Private Circle Mode (closed 2026-05-16)

All 6 sub-slices shipped and archived (Phase 25.0 ADR-024 + 25.1 data spine / migrations 035–037 / §(g.1) `wipe_own_data` amendment + 25.2 visibility picker + 25.3 Connections UI + 25.4 invite redemption + 25.5 Discovery friends-only filter). See [completed-archive.md `## Phase 25.x — Friend-Only / Private Circle Mode`](../docs/en/phase/completed-archive.md#phase-25x--friend-only--private-circle-mode-closed) for full wave detail + parallel-scheduling note + operator-side actions remaining (notably the ADR §Phase 25.5 2-tester E2E smoke test + IARC Q8 re-answer, deferred per the existing pending threads / Phase 40 GA). Promoted from CLAUDE.md inline per the `### Archive policy` wave-close cadence — Phase 25's true close-out was the §(g.1) amendment (commit `6cb1af0`).

### Planned — Phase 26 Auth Infrastructure (OAuth + MFA + Biometric) (closed 2026-05-15)

All 11 sub-slices shipped and archived (Phase 26.0 ADR + 26.1 iOS Apple + 26.2 Android Google + 26.x Apple-on-Android web-OAuth + 26.3 iOS Google + 26.4 linkIdentity + 26.5 TOTP MFA + 26.6 biometric + 26.7 post-OAuth profile gate + 26.8 privacy policy / smoke test / Tech Debt resolutions). See [completed-archive.md `### Phase 26.x — Auth infrastructure`](../docs/en/phase/completed-archive.md#phase-26x--auth-infrastructure-oauth--mfa--biometric--post-oauth-profile-gate-closed) for full wave detail + strategic motivation + operator-side action items (notably the `auth-smoke-test.md` runbook + Data Safety re-submission recipe).

### Planned — Phase 27 Data Wipe (Account-Preserved) (closed 2026-05-16)

All 4 sub-slices shipped and archived (Phase 27.0 ADR-023 + 27.1 migration 033 / `wipe_own_data` RPC + Repository + VM + 27.2 Compose/SwiftUI UI surfaces + post-wipe banner + 27.3 `/data-deletion/` web fallback + 27.4 privacy-policy update). Closed the Phase 39 alpha-launch HARD-GATE item "data-wipe self-service for A0d-6 Data Safety はい answer". See [completed-archive.md `## Phase 27.x — Data Wipe (Account-Preserved)`](../docs/en/phase/completed-archive.md#phase-27x--data-wipe-account-preserved-closed) for full wave detail + the mailto-over-Edge-Function decision + operator-side action items (notably the deferred 6-scenario smoke test + the A21 self-service Edge Function Tech Debt reopen criteria).

### Planned — Phase 28 Video Upload (Pro Feature) — post-alpha, 2026-05-14
Operator flagged 2026-05-14 while filling A0d-6 Data Safety (写真と動画 category): want to enable video upload alongside photos. Storage / bandwidth cost concern → gate behind Pro entitlement (existing Phase 41 F1 RevenueCat infrastructure reusable; no new entitlement key, just `isPro` checks). NOT alpha-launch HARD-GATE — alpha ships with photo-only; Phase 28 lands post-alpha-launch.

- **Phase 28.0** — ADR cut. Decide: (a) supported codecs (MP4 H.264 baseline for broad compatibility); (b) max duration per video (60s? 2min? — balances Storage cost vs feature usefulness for knitting demos); (c) max file size cap (per-video + per-user-quota); (d) thumbnail extraction (client-side via AVFoundation / MediaMetadataRetriever for in-feed preview, OR server-side via Supabase Storage transform); (e) Pro-gating UX (paywall trigger on first video-upload attempt for free users? Or hide the video-add button entirely until upgrade?); (f) moderation extension (existing UGC moderation foundation per ADR-021 — `submit-ugc-report` covers any UGC type; video adds need to handle video-content reporting; potential need for video transcoding to a sanitized format).
- **Phase 28.1** — Server. Supabase Storage bucket for videos with RLS gating (`owner_id = auth.uid()` writes; visibility-aware reads matching Phase 25 friend-only / public state). File-size + duration constraints enforced at the upload-URL signing path. Optional thumbnail generation Edge Function (or rely on client-side extraction).
- **Phase 28.2** — Client + UI. Compose + SwiftUI video-add button on project / pattern detail screens, gated by `isPro` flag. Video picker (Android: `androidx.media3` / Photo Picker; iOS: `PHPicker` for `.movie` types). Upload progress UI. Inline video player on display (Android `ExoPlayer` via `androidx.media3:media3-exoplayer`; iOS `AVPlayer`).
- **Phase 28.3** — Data Safety + privacy policy update. Update A0d-6 Page 3 写真と動画 to check 動画 (becomes 2/2). Re-submit Play Data Safety. Privacy policy `<h3>` subsection added for video data (Supabase Storage path, owner-scoped access, deletion on account-deletion or data-wipe).
- **Phase 28.4** — Smoke test on real device (iOS Sandbox tester + Android license tester, both with Pro entitlement granted via `grant-customer-entitlement`).

**Scheduling**: NOT HARD-GATE for alpha launch. Defaults to post-alpha (beta operation period or pre-Phase-40 GA). Reconsider sequencing if alpha tester feedback signals strong demand.

### Phase 39 W5 GitHub App Bug Report Proxy (per ADR-020) — W5a + W5b ✅
- **Phase 39 W5a** ✅ — Edge Function landing. New `supabase/functions/submit-bug-report/` (Deno + `djwt` JSR + WebCrypto): authenticates as the **Skeinly Feedback** GitHub App with `Issues: Read & write` scope on `b150005/skeinly` only; validates input (title ≤256 + body ≤65,536 + labels ≤5 × ≤50 chars + no newlines in title); applies per-source-hash in-memory rate limit (5 reports / hour, 1-hour sliding window); signs RS256 JWT (PKCS#1 PEM → PKCS#8 ASN.1 wrapping inline so WebCrypto importKey accepts the GitHub-issued .pem; per-instance 9-min cache); exchanges for installation access token (per-instance 55-min cache, 401/403 invalidates both caches); POSTs to `/repos/b150005/skeinly/issues` with default label `feedback`. Closed-enum error code (`RATE_LIMITED` / `VALIDATION_FAILED` / `GITHUB_AUTH_FAILED` / `GITHUB_API_FAILED` / `CONFIG_MISSING`) surfaced as `{ok: false, code, message}` envelope at HTTP 200 — non-200 reserved for Supabase-platform problems (client distinguishes "proxy refused" from "proxy broken"). Phase 39.5 client-side URL prefill **stays live** through W5a — only W5b deletes `BugSubmissionLauncher` and routes via new `BugReportProxyClient`. `supabase/config.toml` registers `[functions.submit-bug-report]` with **`verify_jwt = false`** (ADR-020 §Q4 — unauthenticated-client pattern matching `notify-on-write` and `revenuecat-webhook`; real auth lives downstream at the GitHub API call via the App's three secrets). EF-7 secret trio (`SKEINLY_BUGREPORT_APP_ID` + `SKEINLY_BUGREPORT_INSTALLATION_ID` + `SKEINLY_BUGREPORT_PRIVATE_KEY_PEM`) documented in `release-secrets.md` (EN + JA) — Supabase secret count 6 → 9. **Tests (+32 Deno tests, 101 → 133)**: `github_app.test.ts` 15 (JWT minting + cache + iss-claim encoding, installation token success / 401 cache-clear / 5xx / cache-hit reuse, issue create success / payload-shape / 401 / 422 / 5xx / installation-token-routing / unparseable-response); `index.test.ts` 17 (OPTIONS/GET method handling, CONFIG_MISSING env-absent path, validation matrix, rate limit, happy-path envelope + default-labels-["feedback"] + GitHub error mapping). All tests pass via `deno test --allow-net --allow-env`. **Forward-compat anchors for W5b**: Edge Function deploy is decoupled from client-side cutover — W5a lands the function deployed but unused; W5b's client commit flips routing without coordinated deploy timing. **User-side actions (autonomous-unreachable)**: (1) Create **Skeinly Feedback** GitHub App at https://github.com/settings/apps/new with `Issues: Read & write` only + `Only on this account` scope; (2) Generate private key (.pem); (3) Install App on `b150005/skeinly` only, note Installation ID; (4) Create `feedback` Issue label on `b150005/skeinly` (Issues → Labels → New) — the Edge Function default-applies this label and GitHub returns 422 if it's missing; (5) `supabase secrets set` the trio per `release-secrets.md` EF-7; (6) `supabase functions deploy submit-bug-report`; (7) curl smoke test per `submit-bug-report/README.md` (`-H "apikey: <publishable_key>"`). Pre-push invariants unchanged from Phase 24.6 baseline (no Kotlin / Swift touched; Deno + docs only).
- **Phase 39 W5b** ✅ — KMP client cutover. Phase 39.5's URL prefill path is fully retired; bug reports POST to the `submit-bug-report` Edge Function (deployed by W5a) which authenticates as the **Skeinly Feedback** GitHub App and creates the Issue server-side. Full design + 4 decision points (Q1 URL prefill removal / Q2 public Issues / Q3 in-memory rate limit / Q4 `verify_jwt = false` unauthenticated-client pattern with rejected `verify_jwt = true` alternatives) in [ADR-020](../docs/en/adr/020-phase-39-w5-bug-report-proxy.md). **Deleted**: `BugSubmissionLauncher` (commonMain expect + Android/iOS actuals) — 3 files. **New**: `data/bug/BugReportProxyClient.kt` (commonMain, ~200 LOC) — Ktor HTTP POST sending `apikey: <SupabaseConfig.publishableKey>` (NOT `Authorization: Bearer`; the Edge Function runs with `verify_jwt = false` per ADR-020 §Q4). Returns `Result<SubmitOutcome>` over `BugReportProxyException` sealed hierarchy ({Offline / RateLimited / ValidationFailed / ConfigMissing / Server / Unknown}). Maps Edge Function envelope `{ok, code, message}` → typed exception. Short-circuits to `ConfigMissing` when `supabaseUrl` / `supabasePublishableKey` empty (local-only dev builds). Uses existing `symbolPackHttpClient` Koin qualifier. **ViewModel**: `BugReportPreviewViewModel.submit` signature becomes `suspend (title, body) -> Result<SubmitOutcome>`; state extended with `submitResult: SubmitResultState?` (sealed: `Success(issueNumber, htmlUrl)` / `Error(kind, rawMessage)` where `kind: ErrorKind` is a 6-arm closed enum mirroring the proxy exceptions). Title sent to proxy is `$description` as-is (no `[Beta]` prefix — Phase 40 GA reuses this surface unchanged per ADR-020 §Context); empty description defaults to `"Bug report"`. Submit handler async with idempotency guard against double-tap. New `DismissResult` event clears the banner. **Compose `BugReportPreviewScreen.kt`**: Result banner with Material 3 `Card` (primaryContainer / errorContainer color tokens), CircularProgressIndicator on submit button while in-flight, description field disabled during submission, submit button disabled on success (re-enabled on Error). **SwiftUI `BugReportPreviewScreen.swift`**: parallel banner + ProgressView + .disabled bindings; switch over Kotlin sealed `ErrorKind` via NSEnum bridge with defensive `default` arm. **Edge Function side** (deployed before this commit's KMP cutover): default Issue label `feedback`; `supabase/config.toml` `[functions.submit-bug-report]` `verify_jwt = false`; `computeSourceHash` reads `apikey` then `authorization` fallback as rate-limit seed. **i18n (+10 keys, 440 → 452)**: `state_bug_report_submitting/submitted/error_*` (6 ErrorKind classifications) + `action_bug_report_dismiss_result` + reworded `body_bug_report_includes_actions` ("sends this report to the Skeinly team...") + `action_submit_bug_report` ("Send report"). JA + iOS xcstrings updated. **Koin**: `RepositoryModule` registers `BugReportProxyClient`; `ViewModelModule` binds `proxyClient::submit`; both `PlatformModule.{android,ios}.kt` drop the `BugSubmissionLauncher` registration. **Tests (+24 commonTest, 1512 → 1536)**: `BugReportPreviewViewModelTest` rewritten (19 tests). New `BugReportProxyClientTest` (14 tests, Ktor MockEngine: success / empty-url / empty-key → ConfigMissing; envelope code maps; HTTP 500 / 503 → Server; unparseable JSON → Unknown). New deps: `libs.ktor.client.mock` in commonTest. **Privacy policy** (`docs/public/privacy-policy/index.html` + JA): replaces Phase 39.5 wording with W5b proxy description — Edge Function + Skeinly Feedback GitHub App scope-limited permissions + transit metadata vs Issue-body data distinction + "Issue created by the App, not your personal GitHub account" identity-hygiene point. Pre-push invariants all green: ktlintCheck ✅, compileTestKotlinIosSimulatorArm64 ✅, testAndroidHostTest ✅ (1536 tests), koverVerify ✅, verifyI18nKeys 452 ✅. **User-side actions remaining**: TestFlight + Play Internal smoke test from a real beta build, verifying tap-submit creates an Issue on b150005/skeinly without opening a browser. Server-side curl smoke test green on 2026-05-12 (3-step deploy fix trail documented in ADR-020 revision history: Skeinly Feedback rename + `apikey` header + `verify_jwt = false`).

### Post-v1.0
- **Phase 21**: macOS Target — based on user demand after v1.0

## Tech Debt Backlog

Items deferred across phases. When reopening one, cut a dedicated PR — do not bundle into unrelated feature work unless the commit message calls it out explicitly.

### Chart Editor (Phase 32 follow-ups)
- **i18n sweep across remaining shared screens** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).
- **Maestro `text: "Back"` / `text: "Clear search"` brittleness** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).
- **ViewModel error-message localization** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).
- **Maestro E2E for chart editor** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).
- **Android per-app locale non-functional** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).

### KMP / Compose recurring gotchas
- **Kotlin/Native test-name restriction**: backticked function names containing `(`, `)`, `,` fail `:shared:compileTestKotlinIosSimulatorArm64` / `IosX64` but pass `:shared:testAndroidHostTest` (JVM). Hit Phase 32 CI once (commit 6b65df4 is the fix). Pre-push verification should include the iOS simulator compile target; consider adding to `docs/en/` or a pre-push hook.
- **ktlint multiline formatting**: multiline expressions (e.g. `hasUnsavedChanges = computeUnsavedChanges(...)` with wrapped args) must start the call on a new line. JVM builds tolerate the inline form; CI `ktlintCommonMainSourceSetCheck` rejects it. Pre-push verification should include `./gradlew ktlintCheck` (or at minimum run `ktlintFormat` before committing). Hit Phase 32.2 CI once (commit 739fc82 is the fix).
- **`BackHandler` not in commonMain**: `androidx.activity.compose.BackHandler` cannot be used directly from shared Compose screens. Resolved in Phase 32.4 by adding `ui/platform/SystemBackHandler.kt` (`expect/actual`). Use `SystemBackHandler(enabled, onBack)` from any shared screen that needs to intercept Android system-back. iOS actual is a no-op (iOS uses SwiftUI screens, not shared Compose).

### Phase 26 OAuth follow-ups
- **Compose Google G logo on `OutlinedButton`** (surfaced 2026-05-14 with Phase 26.2): the Phase 26.2 Compose Sign-in-with-Google button uses a plain `OutlinedButton` labeled with the localized string "Continue with Google". Google's branding guidelines require the official Google G logo + Roboto Medium label on the canonical sign-in surface; we deferred the asset migration to avoid pulling in a Material Components branding dep + vector drawable conversion at sub-slice time. Implementation when reopened: add `androidApp/src/main/res/drawable/ic_google_g.xml` (32×32 multi-color vector — converted from Google's published SVG asset), wire as `Icon` slot on the `OutlinedButton`, ensure both light + dark themes render correctly. Estimated scope: ~30 LOC + 1 drawable XML. Lifts cosmetic-only Tech Debt; functional OAuth path is unchanged.
- **iOS CI workflow `IOS_GOOGLE_SERVICES_PLIST_BASE64` decode step** (**CLOSED** 2026-05-14, Phase 26.3): `release.yml` `build-ios` job now `environment: production` scoped + adds a "Decode GoogleService-Info.plist + extract reverse client ID" step that base64-decodes the secret to `iosApp/iosApp/GoogleService-Info.plist` and runs `PlistBuddy -c "Print :REVERSED_CLIENT_ID"` to derive the OAuth URL scheme value. `ci.yml` + `e2e.yml` write `GOOGLE_REVERSE_CLIENT_ID =` empty to `local.xcconfig` (those workflows don't exercise Google Sign-In). The xcconfig variable substitutes into `project.yml`'s `CFBundleURLTypes`; empty value is valid. Trail kept inline only while the Phase 26.3 entry is recent; archive at Phase 40 GA per the archive policy.
- **Skeinly-Dev OAuth Sign-In setup deferred** (surfaced 2026-05-14 with Phase 26.2): Phase 26.2 Google OAuth Clients created in Skeinly Blaze (production Firebase project) only. Skeinly-Dev (debug-variant Firebase project at `androidApp/src/debug/google-services.json`) has NO OAuth Client entries → debug builds with Skeinly-Dev `google-services.json` cannot do Google Sign-In on Android. The shared `OAuthClient.android.kt` reads `R.string.default_web_client_id` lazily inside `acquireGoogleIdToken()`; on Skeinly-Dev that resource resolves to empty string, the actual returns `OAuthIdTokenResult.Failure(...)`, the ViewModel surfaces `ErrorMessage.Generic` to the user. Trade-off accepted because: (a) Phase 39 alpha launch HARD-GATE is release builds only (TestFlight + Play Internal Testing), both consuming Skeinly Blaze creds, (b) dev/prod FCM separation maintained, (c) developer-side dev testing can fall back to email/password OR install a release-build emulator binary. Reopen if developer experience friction surfaces during Phase 26 implementation testing — fix is to create matching OAuth Web + Android + iOS Clients in Cloud Console for Skeinly-Dev's Firebase project and re-download Skeinly-Dev's `google-services.json` (Skeinly-Dev's `FIREBASE_GOOGLE_SERVICES_JSON_BASE64` GitHub Secret in the `development` env would also need a refresh).

### Deprecated-API cleanup (low priority)
- **EncryptedSharedPreferences + MasterKey deprecation** (pre-alpha A14, 2026-05-12, commit `7712136`): `androidx.security:security-crypto:1.1.0` (latest stable) marks `EncryptedSharedPreferences` + `MasterKey` as `@Deprecated` in Java. Google's stated direction is to migrate to direct Android Keystore + AES-GCM key handling, NOT a 1:1 in-package replacement. The deprecated APIs remain functional and load-bearing for the auth-token-at-rest encryption wired in `7712136` (Koin `named("auth")` Settings + `SettingsSessionManager`). `@file:Suppress("DEPRECATION")` at `PlatformModule.android.kt:1` with an explanatory KDoc captures the choice. **Post-alpha migration plan**: replace with Keystore-direct AES-GCM key + Jetpack DataStore-backed encrypted storage. Estimated scope: ~200 LOC + data migration from existing `skeinly_auth_secure.xml` to the new DataStore. Not blocker-level since the current implementation still encrypts at rest with Tink-managed Keystore keys.
- `rememberSwipeToDismissBoxState` — **PARTIAL CLEANUP 2026-05-09** (CMP 1.10.3, this commit). Inner `private fun SwipeToDismiss*Card` / `SwipeToDismissNoteItem` composables in `PatternLibraryScreen.kt:412` / `ProjectListScreen.kt:525` / `ProjectDetailScreen.kt:705` had `@OptIn(ExperimentalMaterial3Api::class, ExperimentalFoundationApi::class)`. Empirically removed `ExperimentalMaterial3Api::class` (kept `ExperimentalFoundationApi::class` for `combinedClickable` on the inner cards) — `:shared:compileTestKotlinIosSimulatorArm64` + `ktlintCheck` + `testAndroidHostTest` + `koverVerify` all green, confirming the SwipeToDismissBox family is structurally stable in CMP 1.10.3's bundled material3 (consistent with the 2026-04-30 upstream verification). **Outer screen `@OptIn(ExperimentalMaterial3Api::class)` intentionally retained** — those screens use `TopAppBar`, which the 2026-05-02 empirical recheck confirmed is still `@ExperimentalMaterial3Api` in CMP 1.10.3. The remaining `@Suppress("DEPRECATION")` on the `confirmValueChange` parameter is an orthogonal Material3 1.7+ deprecation (snapshotFlow + reset() migration is UX-impacting, deferred to a design system audit per the inline comment). Reopen the outer-screen cleanup only after a CMP minor that stabilizes `TopAppBar`.
- **Android Adaptive Icon (Android 8.0+)** — **CLOSED 2026-05-08**. Implemented in the AppIcon brand-refresh slice: `mipmap-anydpi-v26/ic_launcher.xml` + `ic_launcher_round.xml` reference `@mipmap/ic_launcher_foreground` (per-dpi adaptive foreground PNGs authored within the 66dp safe zone) + `@color/ic_launcher_background` (`#EED952` defined in `values/colors.xml`). All 5 dpi buckets (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi) carry `ic_launcher{,_round,_foreground}.png`. Verified via `:androidApp:assembleDebug` BUILD SUCCESSFUL + APK contains `res/mipmap-anydpi-v26/ic_launcher{,_round}.xml` and per-dpi foreground PNGs. Skipped the `ldpi` bucket from the source assets because `minSdk = 26` (the adaptive icon API itself requires 26+, so legacy ldpi devices are out of scope for this app). Source assets shipped from `/Users/b150005/Development/Assets/AppIcon/Skeinly_Adaptive_AppIcon/` — designer-supplied 108dp foreground with central skein-motif on transparent ground.

### Structured chart cosmetic polish
- `jis.crochet.reverse-sc` chevron form — awaiting real-user feedback before changing.

### Discovery (Phase 36 follow-ups)
- **iOS Discovery thumbnail live-render** (**CLOSED** — historical trail in [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md#closed-tech-debt-trails)).

### Pre-alpha (RC) bug bash — Sentry App Hang triage (2026-05-13)

- **Supabase client `requestTimeout` missing** (**CLOSED** 2026-05-13): `SupabaseModule.kt` had no `requestTimeout` set on `SupabaseClientBuilder` — Ktor Darwin engine on iOS (NSURLSession underneath) blocked indefinitely on unreachable hosts, causing sign-up button to "do nothing" from the user's perspective (the coroutine never returned, ViewModel stayed `isSubmitting = true`, alert dialog never fired). Symptom surfaced both as the operator's reproducible real-device sign-up freeze AND as 11 Sentry App Hang issues over the last 19 hours (all dev/simulator environment, bottom-of-stack `writev`/`read`/`open$NOCANCEL`/`madvise` POSIX syscalls). Fix: `requestTimeout = 20.seconds` on the SupabaseClientBuilder DSL — propagates to every Supabase API call (Auth, Postgrest, Realtime, Storage, Functions) since they share the underlying Ktor client. `HttpRequestTimeoutException` now fires within 20 s on any blocked request, routed through `SignUpUseCase.catch(Exception)` → `UseCaseResult.Failure` → `AuthViewModel.error` → SwiftUI/Compose `.alert(title_error)`. New commonTest `sign up timeout propagates to Failure not silent hang` locks in the contract.
- **iOS 26 `UITextSelectionInteraction._handleMultiTapGesture` App Hang** (Sentry `SKEINLY-IOS-3N`, **UPSTREAM**): iOS 26.4 SDK SwiftUI TextField regression — multi-tap gesture handler blocks main thread for >2 s. Not in our code; would need a UIKit-bridging workaround to mitigate. Defer as upstream Apple SDK issue; re-evaluate if it surfaces from real TestFlight users post-alpha.
- **Sentry dSYM upload not configured** (**CLOSED** 2026-05-14): `fastlane-plugin-sentry` wired in `iosApp/fastlane/Pluginfile` + loaded via `eval_gemfile` in `iosApp/Gemfile`. New helper `upload_dsym_to_sentry` in `iosApp/fastlane/Fastfile` invoked from the `beta` lane after `upload_to_testflight`. Reads `SENTRY_AUTH_TOKEN` (Org Auth Token, GitHub Secret §16 in release-secrets.md, already registered) + `SENTRY_ORG` (Sentry org slug, GitHub **Variable** — half-public identifier, new slot §16.5 in release-secrets.md added). Hardcoded `project_slug: skeinly-ios` per README + architecture.md vendor table. Graceful degradation: if either env is unset, logs a warning and skips — TestFlight upload still succeeds, only frame symbolication for that build is missing. CI workflow `.github/workflows/release.yml` extended to pass both env vars from `secrets.SENTRY_AUTH_TOKEN` + `vars.SENTRY_ORG`. **Operator-side action remaining**: register the Sentry org slug as GitHub Variable via `gh variable set SENTRY_ORG --body "<slug>"` (one-time, ~30s). Next TestFlight build after merge symbolicates Sentry App Hang / Crash frames automatically. Android mapping upload mirror remains separate (no `skeinly-android` project frame issue surfaced yet; can wire `sentry-android-gradle-plugin` similarly in a follow-up slice if Android obfuscation becomes a blocker).
- **`🔴 (Supabase-Auth) Failed to load session` log noise** (cosmetic, low priority): supabase-kt 3.6 SDK internal log fires on every fresh launch when the Keychain has no cached session. This is normal first-launch behavior but the `🔴` emoji + ERROR-style prefix mis-signals severity. Filing under cosmetic Tech Debt — supabase-kt's logger is not exposed on the SupabaseClientBuilder DSL we use, so suppression requires either upstream PR to supabase-kt or a runtime logger interceptor we don't currently install. Re-evaluate if testers report confusion.

### Phase 40 GA release prep
- **Bug-report Settings entry GA opening**: Phase 39 W5 (per ADR-020 §Amendment) recognised that the GitHub App (`Skeinly Feedback`) + Edge Function + Issue label (`feedback`) are reused unchanged by general users post-GA, but the **in-app entry points** stay `BuildFlags.isBeta`-gated for Phase 39 closed-beta operation: (a) Settings → Beta → "Send Feedback" row is hidden in `BuildFlags.isBeta = false` builds; (b) Android 3-finger-long-press + iOS shake gestures fire only when `BuildFlags.isBeta && analyticsOptIn`. Phase 40 GA opens at least the Settings entry — move the "Send Feedback" row from the Beta section to a general-purpose section (e.g. "Help & Feedback" or directly under "About"), drop the `BuildFlags.isBeta` gate, drop the `analyticsOptIn` gate on the Settings entry (a user choosing "Send Feedback" through Settings is granting consent at the action moment; opt-in analytics is a separate channel). Gesture-based triggers can remain Beta-only — shake / 3-finger-long-press are debugging affordances best left to testers, not GA users (the shake gesture is an established footgun for general users who use the app while in motion). i18n: section header in Settings may need a new key (e.g. `label_help_feedback_section`) — current "Beta" wording stays only on the Phase 39 build. Compose + SwiftUI screen edits, i18n updates, Settings ViewModel + Tech-Debt closure.
- **iOS `IS_BETA` xcconfig consistency check** (**CLOSED** 2026-05-08): `verifyIosBetaFlag` Gradle task added in `build.gradle.kts` (root) following the `verifyI18nKeys` pattern — reads `version.properties` `VERSION_NAME` major component + `iosApp/project.yml` `IS_BETA` value, asserts `(major == 0) <=> (IS_BETA == "YES")` with a clear remediation message naming both fix paths. Wired into `:shared:check` via `dependsOn(":verifyIosBetaFlag")` in `shared/build.gradle.kts`, into `ci.yml` `shared-checks` step (runs alongside `verifyI18nKeys`), and into `make ci-local`. Verified against current state (`VERSION_NAME=0.1.0` + `IS_BETA="YES"` → OK) and the mismatch path (temporarily flipped `IS_BETA="NO"` → fails with the expected coupled-edit-needed message + remediation). At Phase 40 GA, the task structurally guarantees the coupled `VERSION_NAME 0.X.Y → 1.0.0` + `IS_BETA YES → NO` flip cannot land in two separate commits — a one-sided commit fails CI immediately. Project convention pivoted from shell scripts to Gradle tasks when `verify-i18n-keys.sh` migrated; this implementation follows that pattern (the original Tech Debt entry mentioned `scripts/verify-ios-beta-flag.sh` predates the pivot).
- **SwiftUI Text(…) literal → shared `Res.string.*` semantic key migration** — ✅ **CLOSED 2026-05-16** (commit `b11dbc9`). The 7 (b) allow-listed literals were migrated: 6 in `ChartEditorScreen.swift` → `LocalizedStringKey(<existing shared key>)` (`label_craft_knit`/`_crochet`, `label_reading_knit_flat`/`_crochet_flat`/`_round`, `dialog_unsaved_changes_body` — the same keys Compose ChartEditor already maps). The 7th, "Failed to load image", was **not** a pure orphan — `ChartImageViewer` hardcoded it on BOTH platforms (no shared key), so a new `state_image_load_failed` ("Could not load image" / 画像を読み込めませんでした, parallels `state_profile_load_failed`) was added (en/ja CMP + iOS xcstrings) and wired into Compose + SwiftUI. The 7 (b) entries were removed from `xcstringsAllowedOrphanKeys` (root `build.gradle.kts`) — only the 5 (a) system/format/glyph literals (`%@`, `%@ / %@`, `%@ · %@ · %@`, `?`, `Back`) remain as a permanent skip. The 7 stale auto-extracted orphan entries were deleted from `Localizable.xcstrings`. A regression-guard comment in `build.gradle.kts` flags any future (b)-style re-add as un-localized drift. Net effect: `verifyI18nKeys` now structurally blocks un-localized iOS `Text("literal")` drift (the original Phase-40-GA goal, achieved early). Verified: gradle invariant block (incl. verifyI18nKeys) + ios-build + e2e-android 9/9 green; ios-test/e2e-ios CI-gated (Xcode 26.x local limitation). (Estimate had said ~20 files / ~12 parity entries; actual surface was 7 literals across 3 files + 1 new key — the prior estimate over-counted.)
- **App Store Connect → アプリのアクセシビリティ 申告** (scheduled pre-Phase-40 GA): ASC has a public-product-page surface declaring which assistive-tech features (VoiceOver / 音声コントロール / さらに大きな文字 / ダークインターフェイス / カラー以外で区別 / 十分なコントラスト / 視差効果を減らす / キャプション / バリアフリー音声ガイド) the app supports end-to-end. Phase 39 alpha skips this (TestFlight Internal has no public product page) per vendor-setup.md A0b. At Phase 40 GA prep, audit current a11y posture against each declarable feature: prerequisite tasks are (a) A25 Reduce Motion iOS SwiftUI sweep — ✅ **CLOSED 2026-05-16** (Tech Debt entry above), (b) M5 ChartEditor zoom + WCAG 2.5.8 target-size (active-backlog #post-beta), (c) full VoiceOver + Dynamic Type label-coverage audit across all shared Compose + SwiftUI screens (not yet started), (d) sufficient-contrast verification across Liquid Glass tokens in both light + dark. Declare only what passes the audit; Apple compliance is "verified end-to-end for core tasks", false declarations are App-Review rejectable. Likely first-pass declarations: ダークインターフェイス (clearly supported per Phase 18 Liquid Glass) + 視差効果を減らす (A25 SwiftUI sweep now closed — prerequisite (a) met). Hold VoiceOver / 大きな文字 / 十分なコントラスト / カラー以外で区別 until audit completes.
- **Firebase App Check 有効化** (surfaced 2026-05-14 by Phase 26.2 deliberation): operator declined the App Check checkbox during Phase 26.2 iOS Client creation in Cloud Console. Phase 40 GA prep enables App Check via Firebase Console + iOS App Attest + Android Play Integrity. App Check binds tokens to verified app instances, blocking abuse from cloned / modified APKs / API replay traffic — important once Marketplace ships and account credentials carry monetary value. Estimated scope: ~150 LOC SDK init (iOS `FirebaseAppCheck` + Android `FirebaseAppCheck.installAppCheckProviderFactory`) + new SDK deps + Play Integrity API enablement in Cloud Console + smoke test against Cloud-Console-reported "verified vs unverified" traffic ratio.
- **Apple OAuth client_secret JWT 6-month rotation runbook** (surfaced 2026-05-14 with Phase 26.1): JWT signed for Supabase Apple provider expires every 6 months per Apple spec. Calendar reminder needed — set for 5 months 25 days after the most recent JWT generation. Procedure: re-run Supabase docs in-browser JWT generator with the same `.p8` + Team ID + Key ID + Services ID → paste replacement JWT into Supabase Dashboard → Save. `.p8` itself is permanent (only the JWT rotates). Failure mode if missed: `signInWithApple` returns server-side error visible in Supabase logs as `invalid_client` from Apple's token endpoint; users see a generic error banner on LoginScreen.
- **Play App Signing SHA-1 registration** (surfaced 2026-05-14, Phase 39 alpha launch HARD-GATE addition with Phase 26.2): After the first Play Internal Testing upload, Play Console "アプリの署名" generates the **Play App Signing SHA-1** (different from the upload signing SHA-1 already registered). This Play-issued SHA-1 must be added in Cloud Console → Skeinly Android OAuth Client → Add fingerprint. Without it, Play Store-distributed builds (the actual binary testers receive) fail Google Sign-In because the device-side credential verification doesn't recognize the signing key. The upload-signing SHA-1 still works for `assembleDebug` / `bundleRelease`-from-local-keystore builds. Recipe: Play Console → All apps → Skeinly → Release → Setup → App integrity → App signing → copy the "App signing key certificate" SHA-1 → Cloud Console → APIs & Services → Credentials → Skeinly Android OAuth Client → Add fingerprint → paste → Save. Re-download `google-services.json` afterward + re-set `FIREBASE_GOOGLE_SERVICES_JSON_BASE64` GitHub Secret in the `production` env so CI release builds get the updated config.

### Phase 39 Android Play Internal track auto-publishing (**CLOSED** 2026-05-09)
- **`gradle-play-publisher` plugin 配線** (commit `<this-commit>`): `gradle-play-publisher` 4.0.0 (`com.github.triplet.play`) を `androidApp/build.gradle.kts` に追加。`play { }` ブロックで `track = "internal"` + `releaseStatus = DRAFT` + `defaultToAppBundles = true` を固定。`libs.versions.toml` に plugin alias 追加。
- **`release.yml` 拡張**: `build-android` ジョブに `environment: production` 宣言追加 (Environment-scoped `GOOGLE_PLAY_PUBLISHER_SA_JSON_BASE64` 解決のため)。AAB ビルド (`bundleRelease`) + APK ビルド (`assembleRelease`) 両方を成果物化、SA secret が configured なら base64 デコード → `ANDROID_PUBLISHER_CREDENTIALS` env var に export → `:androidApp:publishBundle` 実行で Internal track Draft アップロード。secret 未設定なら upload step skip + 警告ログ + AAB/APK アーティファクトのみ残す（half-configured release への graceful degradation）。
- **構造的安全装置**: `releaseStatus = DRAFT` で AAB が Play Console 上で Draft release として着地、ユーザーが手動で「テスターに配信」クリックで配信開始。タグ push が誤ってテスターに自動 rollout する事故を防止。SA の Play Console 権限は「テスト版トラックへのリリース」のみ (= Production track への昇格は構造的に不可能)、`releaseStatus` の値に関わらず多段防御。
- **Makefile**: `release-aab-local` ターゲット追加 — タグ push 前にローカルで AAB ビルドの signing chain 検証可能 (アップロードなし)。
- **gradle-play-publisher 4.0.0 + AGP 9.x 互換性確認済み**: ローカル `:androidApp:bundleRelease` BUILD SUCCESSFUL + `:androidApp:tasks --all` で `publishBundle` / `publishApk` / `bootstrapListing` / `promoteReleaseArtifact` 等の Play Publishing タスク登録を確認。
- **動作確認待ち**: 次のタグ push (`v0.1.x`) で end-to-end 検証 (CI で base64 decode → AAB build → upload → Play Console に Draft 着地)。CI ジョブ実行ログで `:androidApp:publishBundle` step が成功し、Play Console 内 Internal track の Draft releases に AAB が現れるかをユーザー側で確認。

### Phase 39 closed-beta sandbox infrastructure (**CLOSED** 2026-05-08)
- **`Purchases.logIn(userId)` 配線** (commit `e1088d1`): `RevenueCatService.identifyUser(userId)` / `logOutUser()` を interface に追加 + `RevenueCatServiceImpl` で `awaitLogInResult` / `awaitLogOutResult` ラップ + 新規 `RevenueCatAuthBridge` top-level fn が `AuthRepository.observeAuthState()` を購読して Authenticated → identify / Unauthenticated → logOut を distinct-until-changed で発火。Application init から `SkeinlyApplication.onCreate` (Android) と `KoinHelperKt.startRevenueCatAuthBridge()` (iOS, `iOSApp.init` 呼び出し) で起動。RevenueCat 失敗は best-effort (println + auth flow uncoupled)。+8 commonTest (1456 total)。
- **`upsert_subscription_from_webhook` RPC** (commit `33c098f`, migration 023): SECURITY DEFINER + `last_verified_at` 順序ガード付き conditional upsert。out-of-order RevenueCat retry が新しい状態を古い状態で上書きできない。closed-enum 再検証 (platform / product_id / status) で defense-in-depth。Prod に `mcp__supabase__apply_migration` で適用済み。
- **`environment` カラム + RPC 拡張** (migration 024): `public.subscriptions` に `environment TEXT NOT NULL DEFAULT 'production' CHECK (... IN ('production', 'sandbox'))` 追加 + `upsert_subscription_from_webhook` RPC に `p_environment` パラメータ追加 (DEFAULT 'production')。`idx_subscriptions_active_production` パーシャルインデックスを併設。Edge Function は `mapEnvironment(event.environment)` で `"SANDBOX"` → `"sandbox"` / `"PRODUCTION"` → `"production"` に lower-case 正規化。Phase 40 GA 後に sandbox dev-test と production traffic が同じテーブルに流入しても `WHERE environment = 'production'` で分析を分離可能。Prod に適用済み。
- **`revenuecat-webhook` Edge Function** (commit `2752a30`): `supabase/functions/revenuecat-webhook/{index,mapping,mapping.test}.ts` + README。constant-time Bearer 検証 + 11 種 RevenueCat event タイプの status マッピング (`INITIAL_PURCHASE` / `RENEWAL` / `CANCELLATION` + cancel_reason variants / `EXPIRATION` / `BILLING_ISSUE` / `REFUND` / etc.)。TEST / SUBSCRIBER_ALIAS / TRANSFER / anonymous app_user_id / 未対応 store / 未マッピング event は 200 で fast-acknowledge (RevenueCat retry を即終了)。+29 Deno tests (`deno test supabase/functions/revenuecat-webhook/`)。デプロイは user-side: `supabase functions deploy revenuecat-webhook`。
- **Sandbox tester onboarding 手順** (commit `<docs-commit>`, [docs/{en,ja}/ops/beta-testing.md](../docs/en/ops/beta-testing.md)): Apple Sandbox tester / Google Play License tester 登録から sandbox accelerated-time での更新サイクル観測まで。closed beta テスター 5–10 名分の invite ワークフローを doc 化。
- **HARD-GATE 残項目** (user-side actions only):
  1. Edge Function deploy: `supabase functions deploy revenuecat-webhook`
  2. RevenueCat dashboard で Webhook URL + Authorization header 設定 ([release-secrets.md EF-5](../docs/ja/release-secrets.md))
  3. dashboard "Send test event" で end-to-end 検証
  4. Apple Sandbox testers + Play License testers 登録 (テスター人数分)
  5. TestFlight + Play Internal Testing への招待

### Phase H delete history (CLOSED 2026-05-09)
- **`verify-receipt` Edge Function 削除完了** (commit this slice): User 方針シフト「Phase H をベータ前に着手すべき」を agent team で deliberation した結果、**RevenueCat が Apple/Google レシート検証を server-side で完結させており、`revenuecat-webhook` Edge Function (Phase 39.0.1 prep, 2026-05-08, commit `2752a30`) が webhook 経路 writer を担っている以上、`verify-receipt` の補完的役割が確立できない**との結論。検討した補完案 (RevenueCat 障害時 fallback / 独立 audit trail / cross-check) は技術的に成立可能だが、いずれも (a) クライアント側 raw receipt 抽出 path 新設で RevenueCat-only 設計を崩す、(b) Apple `.p8` + Google SA キー custody surface 拡大、(c) 5–10 名規模 closed beta では実価値が小さい、の理由で post-beta 判断とした。`supabase/functions/verify-receipt/` (index.ts + README.md) 削除、`Subscription.kt` 等の KDoc を `revenuecat-webhook` に書き換え、`supabase/config.toml` の comment 更新、ADR-016 (en + ja) に amendment block 追記、`release-secrets.md` (en + ja) で EF-4 `GOOGLE_PLAY_IAP_VALIDATOR_SA_JSON` + 「Reused: App Store Connect API key」セクションを DEPRECATED マーク + `unset` コマンド提示 (基底 SA 自体は RevenueCat dashboard が引き続き消費するため SA は revoke しない)。post-beta で実 fail mode 観察後に独立 server-side validator が必要と判断したら新規 ADR (Phase H'仮称) で起票する。
- **基底 SA / GitHub Secrets は不変**: `revenuecat@...` SA は RevenueCat dashboard が引き続き使う (uploaded SA credential として RevenueCat にあり)、`google-play-publisher@...` SA は CI Release アップロード専用で不変、GitHub Secrets §5–§7 (`APP_STORE_CONNECT_API_KEY_BASE64` / `_KEY_ID` / `_ISSUER_ID`) は iOS リリースパイプラインで引き続き必須。Supabase Edge Function 側の secret (`GOOGLE_PLAY_IAP_VALIDATOR_SA_JSON` + `APP_STORE_CONNECT_*`) のみ dormant 化。User-side action: `supabase secrets unset GOOGLE_PLAY_IAP_VALIDATOR_SA_JSON APP_STORE_CONNECT_API_KEY APP_STORE_CONNECT_KEY_ID APP_STORE_CONNECT_ISSUER_ID` (任意、credential surface 削減目的) + `supabase functions delete verify-receipt` (deployed Edge Function を撤去)。

### Accessibility deferrals (post-Phase 39 beta)
- **ChartEditor zoom + WCAG 2.5.8 (Target Size Minimum)** — Sprint B audit (M5) flagged that on large grids (e.g. 16×16+, polar with ≥32 stitches/ring), the editor's auto-fit `cellSize = min(canvasW/gridW, canvasH/gridH)` math drops below the 24×24 SC 2.5.8 minimum touch target. The current editor has NO zoom feature (only `ChartViewerScreen` and `ChartDiffScreen` carry `rememberTransformableState`); the audit's recommended fix ("zoom-to-fit IconButton with explicit accessibilityLabel") requires building zoom from scratch on the editor: `transformableState` + scale/offset in remember state + `Modifier.graphicsLayer` on Canvas + an inverse hit-test transform from screen-space tap coords back to grid coords through the active scale/offset, plus 1–2 IconButtons in the TopAppBar (zoom-in / zoom-out, or zoom-to-fit toggle). Real risk surface in Phase 39 beta is bounded — the default 8×8 grid renders ~40dp cells on phones (well above 24×24), and polar editing is itself deferred to Phase 35.2+. Beta testers authoring 16×16+ rect grids would hit it; mitigation today is to recommend landscape orientation. Reopen this after Phase 39 closes if beta feedback surfaces real usability complaints — it's a meaningful feature add (estimated ~150 LOC Compose + ~100 LOC SwiftUI) and bundling it into a polish sprint risked shipping new bugs to a beta-bound editor.

### Future opportunistic crochet catalog additions (post-30.4)
- Foundation stitches (`fsc`, `fdc`) — US-dominant convention; defer until a JP-pattern user reports needing them.
- Extended sc (`exsc`) — CYC-standard, JIS silent.
- Spike stitches (long-loop variants) — colorwork crochet.
- `turning-ch-N` family — ADR-009 §9 open question (parametric vs. discrete family); reopens when a concrete non-dc-height turning chain shows up in a user pattern.

### Documentation hygiene
- **CLAUDE.md slimming toward ≤300 lines**: Phase 1–37.x and Phase 41.x history already promoted to [docs/en/phase/completed-archive.md](../docs/en/phase/completed-archive.md) per the `### Archive policy` cadence (Phase 41.x archived 2026-05-08 after the policy was relaxed from "Phase 40 GA only" to "wave-close OR major-release, whichever comes first"). CLAUDE.md still carries the Phase 38.x + 39.x bullets because Phase 39 closed-beta is active and Phase 38 sits in the same wave-close grouping. Goal: at Phase 40 GA, promote Phase 38.x + 39.x in the same major-release commit per the existing archive policy. The structural sections (Project intro, Architecture Principles, Tech Stack, Workflow, Specifications, Tech Debt Backlog) stay inline.
